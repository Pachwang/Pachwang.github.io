<?xml version="1.0" encoding="utf-8"?>
<search>
  
  
  
  <entry>
    <title>Docker安装Redis</title>
    <link href="/2023/05/03/Docker%E5%AE%89%E8%A3%85Redis/"/>
    <url>/2023/05/03/Docker%E5%AE%89%E8%A3%85Redis/</url>
    
    <content type="html"><![CDATA[<h1 id="详细步骤"><a href="#详细步骤" class="headerlink" title="详细步骤"></a>详细步骤</h1><ol><li><p>拉取最新的Redis镜像</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs shell">docker pull redis<br></code></pre></td></tr></table></figure></li><li><p>在服务器下创建如下文件夹</p><p>&#x2F;mydata&#x2F;redis&#x2F;myredis&#x2F;data</p><p>&#x2F;mydata&#x2F;redis&#x2F;myredis&#x2F;myredis.conf</p></li><li><p>去官网下载一份标准的配置文件</p><p>官网：<a href="http://www.redis.cn/download.html">http://www.redis.cn/download.html</a></p><p>主要修改如下几项</p><p><code>bind 127.0.0.1</code>  注释掉这部分，使redis可以外部访问 </p><p><code>daemonize no</code> 用守护线程的方式启动 </p><p><code>requirepass 你的密码</code> 给redis设置密码 </p><p><code>appendonly yes</code> redis持久化　　默认是no </p><p><code>tcp-keepalive 300</code> 防止出现远程主机强迫关闭了一个现有的连接的错误 默认是300</p></li><li><p>启动Redis容器</p><p>PS: requirepass 密码自己设置</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs shell">docker run --restart=always -p 6379:6379 --name redis -v /mydata/redis/myredis/myredis.conf:/etc/redis/redis.conf -v /mydata/redis/myredis/data:/data -d redis redis-server /etc/redis/redis.conf  --appendonly yes  --requirepass 123456<br></code></pre></td></tr></table></figure></li><li><p>参数解释</p><ul><li><code>--restart=always</code> 总是开机启动</li><li><code>--log</code>是日志方面的</li><li><code>-p 6379:6379</code> 将6379端口挂载出去</li><li><code>--name</code> 给这个容器取一个名字</li><li><code>-v</code> 数据卷挂载<br><code>- /mydata/redis/myredis/myredis.conf:/etc/redis/redis.conf</code> 这里是将 linux 路径下的myredis.conf 和redis下的redis.conf 挂载在一起。<br><code>- /mydata/redis/myredis/data:/data</code> 同上</li><li><code>-d redis</code> 表示后台启动redis</li><li><code>redis-server /etc/redis/redis.conf</code> 以配置文件启动redis，加载容器内的conf文件，最终找到的是挂载的目录 <code>/etc/redis/redis.conf</code> 也就是linux 下的&#x2F;mydata&#x2F;redis&#x2F;myredis&#x2F;myredis.conf</li><li>–appendonly yes 开启redis 持久化</li><li>–requirepass 设置密码</li></ul></li><li><p>查看容器运行状况</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs shell">docker ps -a |grep redis <br></code></pre></td></tr></table></figure><p><img src="/images/docker007.jpg"></p></li><li><p>RESP客户端测试连接</p><p><img src="/images/docker008.jpg"></p></li><li><p>进入redis容器</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs shell">docker exec -it redis /bin/bash<br>redis-cli<br></code></pre></td></tr></table></figure><p>测试</p><p><img src="/images/docker009.jpg"></p></li></ol><h1 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h1><p><a href="https://blog.csdn.net/weixin_45821811/article/details/116211724">https://blog.csdn.net/weixin_45821811/article/details/116211724</a></p><p><a href="https://cloud.tencent.com/developer/article/1670205">https://cloud.tencent.com/developer/article/1670205</a></p>]]></content>
    
    
    <categories>
      
      <category>Linux</category>
      
      <category>Docker</category>
      
    </categories>
    
    
  </entry>
  
  
  
  <entry>
    <title>Linux总复习</title>
    <link href="/2023/05/02/Linux%E6%80%BB%E5%A4%8D%E4%B9%A0/"/>
    <url>/2023/05/02/Linux%E6%80%BB%E5%A4%8D%E4%B9%A0/</url>
    
    <content type="html"><![CDATA[<h1 id="Linux总复习"><a href="#Linux总复习" class="headerlink" title="Linux总复习"></a>Linux总复习</h1><h2 id="第一讲"><a href="#第一讲" class="headerlink" title="第一讲"></a>第一讲</h2><h3 id="起源"><a href="#起源" class="headerlink" title="起源"></a>起源</h3><p>Linux的起源可以追溯到1991年，当时一位名叫Linus Torvalds的芬兰大学生开始为自己的个人电脑开发一种新的操作系统。这个系统最初是为了满足他在学校计算机课程中的需求而设计的。在他的学习和开发过程中，他使用了Minix操作系统的代码和想法，并结合了自己的创新，最终开发出了一种新的操作系统，也就是现在所知道的Linux。</p><h3 id="历史"><a href="#历史" class="headerlink" title="历史"></a>历史</h3><ol><li>1991年-1993年：最初的开发阶段 在1991年，Linus Torvalds开始开发Linux操作系统的内核，并在1992年发布了第一个可用版本。此时Linux还只是一个小型项目，只有几千行代码。在这个阶段，Linux主要用于个人电脑和服务器领域。</li><li>1994年-1999年：Linux的商业化和扩展 在1994年，Red Hat公司成立，开始将Linux作为商业产品销售。同时，其他公司也开始对Linux进行开发和支持，包括IBM、Novell和Oracle等。在这个阶段，Linux不断扩展，支持更多的硬件和应用程序，并逐渐进入企业市场。</li><li>2000年-2007年：Linux的稳定性和成熟度提高 在2000年代初，Linux开始受到更多企业的青睐，并逐渐被用于一些重要的应用场景，如金融、医疗和航空等领域。此时，Linux的稳定性和成熟度得到了极大的提高，并且逐渐成为企业级操作系统的首选。</li><li>2008年-至今：Linux的广泛应用和开源生态系统的壮大 在2008年之后，Linux得到了更广泛的应用，包括移动设备、嵌入式系统和云计算等领域。同时，Linux的开源生态系统也逐渐壮大，包括各种开源软件和工具，如Docker、Kubernetes、OpenStack等。这些开源技术对现代计算产生了重要的影响，并且Linux已经成为许多企业和组织的核心技术。</li></ol><h3 id="特点"><a href="#特点" class="headerlink" title="特点"></a>特点</h3><ol><li><p>开放性（遵循标准）</p><p>Linux是一种开源的操作系统，任何人都可以查看、修改、使用和分发它的源代码。这意味着Linux的用户可以自由地定制和配置它，以适应他们的需求。</p></li><li><p>多用户、多任务</p><p>Linux可以支持多个用户同时登录和使用同一台计算机，并且可以在后台同时运行多个程序和任务，不会因为资源竞争而出现系统崩溃。</p></li><li><p>良好的用户界面</p></li><li><p>设备独立性（把外部设备当作文件处理）</p><p>Linux的一个重要特点是设备独立性，它能够把外部设备（如硬盘、光驱、打印机、串口、USB设备等）当作文件处理。这意味着Linux把这些设备看做是文件系统中的一个文件，用户可以使用标准的文件操作函数来访问这些设备，而不需要了解它们的具体硬件细节。</p><p>在Linux中，每个设备都有一个对应的设备文件，这些文件被存储在&#x2F;dev目录下，用户可以通过这些设备文件来访问和操作设备。例如，硬盘的设备文件为&#x2F;dev&#x2F;hd<em>（其中</em>表示硬盘的分区），打印机的设备文件为&#x2F;dev&#x2F;lp<em>（其中</em>表示打印机的编号）等。</p><p>通过这种设备独立性的机制，Linux操作系统能够兼容多种不同的外部设备，并且可以很容易地进行扩展和升级。另外，由于外部设备被抽象为文件，因此用户可以使用标准的文件操作函数来进行读写、复制、移动、重命名等操作，这大大简化了对设备的操作和管理。</p></li><li><p>丰富的网络功能</p><ul><li>网络协议支持：Linux支持多种网络协议，包括TCP&#x2F;IP、UDP、ICMP、HTTP、FTP、SSH等，这些协议使得Linux能够在各种网络环境中进行通信和数据传输。</li><li>路由和网络管理：Linux提供了强大的路由和网络管理功能，使得管理员可以轻松地管理和监控网络连接、带宽、流量、安全等方面。</li><li>网络安全：Linux具有多层次的网络安全功能，包括防火墙、网络认证、加密和虚拟专用网络（VPN）等，能够保障网络的安全性和可靠性。</li><li>服务器和服务：Linux可以作为一个网络服务器，提供各种服务，包括Web服务器、邮件服务器、文件服务器、数据库服务器、DNS服务器等，这些服务能够满足不同用户的需求。</li><li>网络性能优化：Linux提供了丰富的网络性能优化功能，包括内核优化、网络调整、缓存机制等，可以提高网络的速度和稳定性。</li></ul></li><li><p>可靠的系统安全</p><p>Linux是一个非常稳定和可靠的操作系统，通常不会出现崩溃或死机等问题。同时，Linux也被认为是一个相对安全的操作系统，因为它的内核可以隔离和保护用户的数据和程序。</p></li><li><p>良好的可移植性</p><p>Linux可以运行在各种不同的硬件和平台上，包括个人电脑、服务器、嵌入式设备和移动设备等。同时，许多Linux应用程序也可以在不同的平台之间移植和运行。</p></li></ol><h3 id="定义"><a href="#定义" class="headerlink" title="定义"></a>定义</h3><p>Linux是一个类UNIX内核的可以自由发布的实现版本，是一个操作系统的底层核心（内核）</p><p>Linux &#x3D;&gt; 内核</p><p>Linux系统&#x3D;&gt;内核+实用工具  </p><p>Linux发行版&#x3D;&gt;内核+配套软件</p><h3 id="GNU-GPL"><a href="#GNU-GPL" class="headerlink" title="GNU GPL"></a>GNU GPL</h3><p>GNU GPL（GNU General Public License）是一种自由软件许可证，是由自由软件基金会（Free Software Foundation，FSF）发起的。该许可证规定，任何使用GNU GPL授权的软件必须在其源代码公开发布的同时，同样采用GNU GPL许可证，也就是说，这些软件必须是开源软件，并且允许其他人自由地使用、修改和再分发。GNU GPL还规定，任何使用该软件的人必须提供原始的源代码和版权声明，并在修改后的软件中明确说明。</p><p>GNU GPL在开源软件界中具有广泛的应用，许多著名的开源软件和操作系统，如Linux操作系统、GNU工具集、MySQL数据库等，都采用了GNU GPL许可证。此外，许多开源社区和组织也采用GNU GPL作为其开源软件的许可证，以保障开源软件的自由和开放性。</p><p>总的来说，GNU GPL是一种为保护自由软件而设计的开源软件许可证，它旨在鼓励开源社区的自由合作、共享和创新，使得开源软件的生态系统更加健康、稳定和可持续。</p><h3 id="常见的发行版本"><a href="#常见的发行版本" class="headerlink" title="常见的发行版本"></a>常见的发行版本</h3><ol><li>Ubuntu：一款非常流行的Linux发行版本，以易用性和友好的界面而著称，适用于桌面和服务器环境。</li><li>Debian：一个稳定、可靠的Linux发行版本，以其强大的软件包管理系统和安全性而著称，适用于服务器和桌面环境。</li><li>CentOS：一款基于Red Hat Enterprise Linux（RHEL）源代码的社区发行版本，以其稳定性和安全性而著称，适用于服务器环境。</li><li>Fedora：一个面向开发者和技术爱好者的Linux发行版本，以其先进的软件包管理系统和最新的技术特性而著称，适用于桌面和服务器环境。</li><li>Arch Linux：一个自由、轻量级的Linux发行版本，以其极简主义和灵活性而著称，适用于技术爱好者和高级用户。</li><li>openSUSE：一个稳定、易用的Linux发行版本，以其强大的YAST系统配置工具和开放性而著称，适用于桌面和服务器环境。</li><li>Kali Linux：一款专门用于网络安全和渗透测试的Linux发行版本，以其预装了大量安全工具而著称，适用于安全测试人员和渗透测试爱好者。</li><li>Red Hat Enterprise Linux（RHEL）：这是一款面向企业级用户的Linux发行版本，以其高度的稳定性、安全性和可靠性而著称。RHEL提供了一系列的服务和工具，如软件包管理、安全更新、系统配置、集成虚拟化等，可以满足企业级用户对于Linux操作系统的各种需求。</li></ol><h3 id="商业、共享、自由和免费软件的区别和联系"><a href="#商业、共享、自由和免费软件的区别和联系" class="headerlink" title="商业、共享、自由和免费软件的区别和联系"></a>商业、共享、自由和免费软件的区别和联系</h3><ol><li>商业软件：商业软件是指由商业公司或组织开发并销售的软件。商业软件通常需要付费购买或使用，其源代码不公开，用户无权对其进行修改或分发。商业软件通常具有稳定性和专业性，适合用于商业、工业和科学领域等。</li><li>共享软件：共享软件是指由开发者免费提供、允许复制和分发的软件。共享软件可以是开源或闭源的，通常允许用户对其进行修改和定制，但需要遵守软件许可协议中的规定。共享软件的目的是鼓励创新和合作，推动知识和技术的自由流通。</li><li>自由软件：自由软件是指用户可以自由使用、复制、修改和分发的软件。自由软件的源代码必须公开，用户有权修改和改进该软件，并且需要将改进后的版本公开发布。自由软件的目的是保护用户的自由和隐私，促进软件的互联互通和技术的共享。</li><li>免费软件：免费软件是指用户可以免费获取和使用的软件，其源代码可以是公开或闭源的，用户无权修改和分发该软件。免费软件的目的是提供一个免费的替代品，以满足用户的一些基本需求，如文字处理、绘图等。</li></ol><h2 id="第二讲"><a href="#第二讲" class="headerlink" title="第二讲"></a>第二讲</h2><h3 id="Linux内核版本号"><a href="#Linux内核版本号" class="headerlink" title="Linux内核版本号"></a>Linux内核版本号</h3><p>由Linus等人制定和维护，全球统一</p><p>内核版本号格式：<strong>x.y.zz</strong></p><p>x为主版本号y为次版本号zz为次次版本号</p><p>例如：</p><p>查看内核版本号：</p><p><img src="/images/linux001.jpg"></p><p>查看发行版本号：</p><p><img src="/images/linux002.jpg"></p><p>2.6及以下版本(x.y.zz)</p><p>次版本号&#x3D;偶数 &#x3D;&gt; 稳定版本</p><p>次版本号&#x3D;奇数 &#x3D;&gt;测试版本</p><p>3.0开始</p><p>次版本号不再表示一个内核是稳定版本还是测试版本，所有发布出来的正式版本<strong>都是稳定版本</strong></p><h3 id="Linux目录结构"><a href="#Linux目录结构" class="headerlink" title="Linux目录结构"></a>Linux目录结构</h3><p>Linux以目录的方式来组织和管理系统中的所有文件，以根目录 &#x2F; 为起点，所有其他的目录都由根目录派生而来，采用了一种层次化的树形结构，称为文件系统层次结构（Filesystem Hierarchy Standard，FHS）。</p><h4 id="常见目录的作用和存放内容"><a href="#常见目录的作用和存放内容" class="headerlink" title="常见目录的作用和存放内容"></a>常见目录的作用和存放内容</h4><ol><li>&#x2F;bin：常用系统程序目录</li><li>&#x2F;boot：开机设定目录，也是存放核心 vmlinuz 的地方</li><li>&#x2F;dev：存放系统设备装置文件的目录</li><li>&#x2F;etc：系统配置文件，尤其 passwd, shadow</li><li>&#x2F;etc&#x2F;rc.d&#x2F;init.d：存放系统开机时载入服务的脚本</li><li>&#x2F;home：系统使用者的目录</li><li>&#x2F;lib：Linux 执行或编译程序函数库目录</li><li>&#x2F;mnt：软驱与光驱接入挂载的地方</li><li>&#x2F;proc：系统核心与执行程序的一些信息</li><li>&#x2F;root：系统管理员的目录</li><li>&#x2F;usr&#x2F;bin, &#x2F;bin：一般执行文件摆放的地方</li><li>&#x2F;usr&#x2F;sbin, &#x2F;sbin：系统管理员常用指令集</li><li>&#x2F;var：摆放系统日志文件的地方</li><li>&#x2F;lost+fount：摆放系统不正常产生错误时遗失的片段</li></ol><h3 id="Linux系统结构"><a href="#Linux系统结构" class="headerlink" title="Linux系统结构"></a>Linux系统结构</h3><p>分为用户空间和内核空间。</p><p>用户空间有应用程序、GUN C函数库。</p><p>内核空间有系统调用接口、内核、依赖于体系结构的内核代码。</p><p><img src="/images/linux003.jpg"></p><h4 id="主要组件"><a href="#主要组件" class="headerlink" title="主要组件"></a>主要组件</h4><p><img src="/images/linux004.jpg"></p><h3 id="关机与重启"><a href="#关机与重启" class="headerlink" title="关机与重启"></a>关机与重启</h3><h4 id="关机"><a href="#关机" class="headerlink" title="关机"></a>关机</h4><ol><li><strong>shutdown –h now</strong></li><li>**init 0 **</li><li>**halt -p **</li><li>**poweroff -p **</li></ol><h5 id="shutdown命令"><a href="#shutdown命令" class="headerlink" title="shutdown命令"></a>shutdown命令</h5><h6 id="作用"><a href="#作用" class="headerlink" title="作用"></a>作用</h6><p>​关机</p><h6 id="格式"><a href="#格式" class="headerlink" title="格式"></a>格式</h6><p>​shutdown [选项] 时间 [消息]</p><table><thead><tr><th align="left">选项</th><th>描述</th></tr></thead><tbody><tr><td align="left">-t  seconds</td><td>设定在几秒钟之后进行关机程序</td></tr><tr><td align="left">-k</td><td>并不会真的关机，只是将警告讯息传送给所有使用者</td></tr><tr><td align="left">-r</td><td>关机后重新开机</td></tr><tr><td align="left">-h</td><td>关机后停机</td></tr><tr><td align="left">-n</td><td>不采用正常程序来关机，用强迫的方式杀掉所有执行中的程序后自行关机</td></tr><tr><td align="left">-c</td><td>取消目前已经进行中的关机动作</td></tr><tr><td align="left">time</td><td>设定关机的时间</td></tr><tr><td align="left">message</td><td>传送给所有使用者的警告讯息</td></tr></tbody></table><h6 id="例子"><a href="#例子" class="headerlink" title="例子"></a>例子</h6><ul><li>立即关机</li></ul><p># shutdown -h now</p><ul><li>指定10分钟后关机</li></ul><p># shutdown -h 10</p><ul><li>在关机动作触发前取消关机动作</li></ul><p># shutdown -c</p><ul><li>重新启动计算机</li></ul><p># shutdown -r now</p><h5 id="init命令"><a href="#init命令" class="headerlink" title="init命令"></a>init命令</h5><h6 id="作用-1"><a href="#作用-1" class="headerlink" title="作用"></a>作用</h6><p>​切换运行模式</p><h6 id="格式-1"><a href="#格式-1" class="headerlink" title="格式"></a>格式</h6><p>​init  运行级别代码</p><table><thead><tr><th>代码</th><th>描述</th></tr></thead><tbody><tr><td>0</td><td>关机级别</td></tr><tr><td>1</td><td>单用户运行级别</td></tr><tr><td>2</td><td>多用户，字符模式，但系统不会启动NFS</td></tr><tr><td>3</td><td>多用户，字符模式，系统启动具有网络功能，常用运行级别</td></tr><tr><td>4</td><td>用户自定义级别</td></tr><tr><td>5</td><td>图形界面模式，常用运行级别</td></tr><tr><td>6</td><td>重启级别</td></tr></tbody></table><h5 id="halt命令"><a href="#halt命令" class="headerlink" title="halt命令"></a>halt命令</h5><h6 id="作用-2"><a href="#作用-2" class="headerlink" title="作用"></a>作用</h6><p>​关机。先检测系统的runlevel，若runlevel为0或6，则关闭系统，否则即调用shutdown -h来关闭系统</p><h6 id="格式-2"><a href="#格式-2" class="headerlink" title="格式"></a>格式</h6><p>​halt [选项]</p><table><thead><tr><th>选项</th><th>描述</th></tr></thead><tbody><tr><td>-d</td><td>不在wtmp中记录（记录开关机等信息的二进制文件）</td></tr><tr><td>-f</td><td>不论目前的runlevel为何，不调用shutdown即强制关闭系统</td></tr><tr><td>-i</td><td>在halt之前，关闭全部的网络界面</td></tr><tr><td>-n</td><td>halt前，不用先执行sync</td></tr><tr><td>-p</td><td>halt之后，执行poweroff关闭电源</td></tr><tr><td>-w</td><td>取消目前已经进行中的关机动作</td></tr></tbody></table><h6 id="例子-1"><a href="#例子-1" class="headerlink" title="例子"></a>例子</h6><p>关闭系统后关闭电源</p><p># halt -p</p><p>关闭系统，但不留下日记记录</p><p># halt -d</p><h4 id="重启"><a href="#重启" class="headerlink" title="重启"></a>重启</h4><ol><li><strong>shutdown -r now</strong></li><li><strong>init 6</strong></li><li><strong>reboot</strong></li></ol><h5 id="reboot命令"><a href="#reboot命令" class="headerlink" title="reboot命令"></a>reboot命令</h5><h6 id="作用-3"><a href="#作用-3" class="headerlink" title="作用"></a>作用</h6><p>​重新启动计算机。若系统的 runlevel 为 0 或 6 ，则重新开机，否则以 shutdown 指令（加上 -r 参数）来取代</p><h6 id="格式-3"><a href="#格式-3" class="headerlink" title="格式"></a>格式</h6><p>​reboot [选项]</p><table><thead><tr><th>选项</th><th>描述</th></tr></thead><tbody><tr><td>-d</td><td>不把记录写到  wtmp 档案里</td></tr><tr><td>-f</td><td>强迫重开机，不呼叫  shutdown 这个指令</td></tr><tr><td>-i</td><td>在重开机之前，关闭全部的网络界面</td></tr><tr><td>-n</td><td>在重开机前不做将记忆体资料写回硬盘的动作</td></tr><tr><td>-w</td><td>并不会真的重开机，只是把记录写到 &#x2F;var&#x2F;log&#x2F;wtmp 档案里</td></tr></tbody></table><h3 id="系统说明文档查询"><a href="#系统说明文档查询" class="headerlink" title="系统说明文档查询"></a>系统说明文档查询</h3><h4 id="man-x2F-info命令"><a href="#man-x2F-info命令" class="headerlink" title="man&#x2F;info命令"></a>man&#x2F;info命令</h4><h5 id="格式-4"><a href="#格式-4" class="headerlink" title="格式"></a>格式</h5><p>man xxx | info xxx</p><ul><li>回车 下一行 </li><li>空格 下一页</li><li>q 退出</li></ul><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs shell"><span class="hljs-meta prompt_"># </span><span class="language-bash">安装命令</span><br>yum install -y man man-pages<br></code></pre></td></tr></table></figure><h5 id="例子-2"><a href="#例子-2" class="headerlink" title="例子"></a>例子</h5><p># man ls</p><h4 id="–help或者-h选项"><a href="#–help或者-h选项" class="headerlink" title="–help或者-h选项"></a>–help或者-h选项</h4><h2 id="第三讲"><a href="#第三讲" class="headerlink" title="第三讲"></a>第三讲</h2><h3 id="Linux交互方式"><a href="#Linux交互方式" class="headerlink" title="Linux交互方式"></a>Linux交互方式</h3><p><img src="/images/linux005.jpg"></p><h3 id="Shell"><a href="#Shell" class="headerlink" title="Shell"></a>Shell</h3><p>Shell是一个作为用户与Linux系统间接口的程序，它允许用户向操作系统输入需要执行的命令，返回执行结果</p><h4 id="种类"><a href="#种类" class="headerlink" title="种类"></a>种类</h4><ol><li>ash：是贝尔实验室开发的Shell，它是轻量级的Shell，用于嵌入式设备或资源受限的系统中。bsh是对ash的符号链接。</li><li>bash：是GNU的Bourne Again Shell，是Linux和其他Unix操作系统上最常用的Shell之一。它包括了Bourne Shell的所有功能，并且添加了许多扩展功能。在大多数Linux发行版中，bash是默认的Shell。sh以及bash2都是对它的符号链接。</li><li>tcsh：是Berkeley UNIX C Shell，是C Shell的增强版本。它包括了C Shell的所有功能，并且添加了许多新特性，如命令补全、历史记录和别名等。csh是对它的符号链接。</li><li>ksh：Korn Shell是由AT&amp;T Bell实验室的David Korn开发的Shell。它的语法与Bourne Shell相同，同时具备了C Shell的易用特点。ksh在Unix系统中得到广泛使用，并且是POSIX标准的一部分。</li><li>zsh：Z Shell是Korn Shell的一个增强版本，它具备bash Shell的许多特点。zsh包括了自动补全、历史记录、别名扩展、动态加载模块等许多功能，使得它成为一个强大的Shell。在许多Linux发行版中，zsh已经成为一个常见的替代品。</li></ol><h4 id="基本格式"><a href="#基本格式" class="headerlink" title="基本格式"></a>基本格式</h4><p>命令名  [选项]   &lt;参数1&gt; &lt;参数2&gt; ……</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs shell">ls -al /usr /var/log /etc<br></code></pre></td></tr></table></figure><h4 id="功能"><a href="#功能" class="headerlink" title="功能"></a>功能</h4><h5 id="Shell提示符"><a href="#Shell提示符" class="headerlink" title="Shell提示符"></a>Shell提示符</h5><p>Shell提示符通常用于在命令行界面显示当前用户的用户名、主机名和工作目录等信息。在大多数Unix和Linux系统中，root用户的提示符是#，而其他用户的提示符是$。</p><h5 id="命令自动补齐"><a href="#命令自动补齐" class="headerlink" title="命令自动补齐"></a>命令自动补齐</h5><p>命令自动补齐是一种Shell特性，可以在输入命令时按下Tab键，自动补齐命令名称、文件名和路径等信息，提高了命令输入的效率。</p><h5 id="历史记录查看"><a href="#历史记录查看" class="headerlink" title="历史记录查看"></a>历史记录查看</h5><p>历史记录是Shell保存用户执行的命令的列表，可以通过键盘上下键或者使用history命令查看之前执行的命令，并且可以重新执行之前执行的命令，提高了命令输入的效率。</p><h5 id="输入输出重定向"><a href="#输入输出重定向" class="headerlink" title="输入输出重定向"></a>输入输出重定向</h5><p>输入输出重定向是一种Shell特性，可以将命令的输入或输出重定向到文件或其他命令中。符号&gt;表示将命令的输出重定向到一个文件中，符号&gt;&gt;表示将输出追加到文件的末尾，符号&lt;表示将文件作为命令的输入。</p><p>以下是一些关于输入输出重定向的例子：</p><h6 id="将命令的输出重定向到文件中"><a href="#将命令的输出重定向到文件中" class="headerlink" title="将命令的输出重定向到文件中"></a>将命令的输出重定向到文件中</h6><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs shell">ls -l &gt; file.txt<br></code></pre></td></tr></table></figure><p>这个命令将当前目录下的文件列表输出到一个名为file.txt的文件中，如果file.txt已经存在，则会覆盖原来的内容。</p><h6 id="将命令的输出追加到文件末尾"><a href="#将命令的输出追加到文件末尾" class="headerlink" title="将命令的输出追加到文件末尾"></a>将命令的输出追加到文件末尾</h6><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs shell">echo &quot;hello world&quot; &gt;&gt; file.txt<br></code></pre></td></tr></table></figure><p>这个命令将字符串”hello world”追加到一个名为file.txt的文件末尾，如果file.txt不存在，则会创建该文件。</p><h6 id="将文件作为命令的输入"><a href="#将文件作为命令的输入" class="headerlink" title="将文件作为命令的输入"></a>将文件作为命令的输入</h6><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs shell">sort &lt; file.txt<br></code></pre></td></tr></table></figure><p>这个命令将一个名为file.txt的文件作为输入，然后使用sort命令对其进行排序，并将结果输出到标准输出。</p><p>除了常见的输入重定向，还有一些命令可以使用”&lt;”符号来读取文件作为输入。下面是一些常见的使用”&lt;”符号的命令及其用法：</p><ul><li>cat命令：用于连接和显示文件的内容，可以使用”&lt;”符号将文件作为输入读取。</li></ul><p>示例：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs shell">cat &lt; file.txt<br></code></pre></td></tr></table></figure><ul><li>sort命令：用于排序文件中的行，可以使用”&lt;”符号将文件作为输入读取。</li></ul><p>示例：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs shell">sort &lt; file.txt<br></code></pre></td></tr></table></figure><ul><li>grep命令：用于在文件中搜索指定的字符串，可以使用”&lt;”符号将文件作为输入读取。</li></ul><p>示例：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs shell">grep &quot;pattern&quot; &lt; file.txt<br></code></pre></td></tr></table></figure><ul><li>head和tail命令：用于显示文件的前几行或后几行，可以使用”&lt;”符号将文件作为输入读取。</li></ul><p>示例：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs shell">head -n 10 &lt; file.txt  # 显示文件的前10行<br>tail -n 20 &lt; file.txt  # 显示文件的后20行<br></code></pre></td></tr></table></figure><ul><li>用于命令的输出</li></ul><p>需要注意的是，这些命令的参数和使用方式可能因具体情况而异。此外，输入重定向符号”&lt;”不仅可以用于文件，也可以用于命令的输出。例如，将一个命令的输出作为另一个命令的输入：</p><figure class="highlight jboss-cli"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs jboss-cli"><span class="hljs-keyword">command</span>1 | <span class="hljs-keyword">command</span>2 &lt; input.txt<br></code></pre></td></tr></table></figure><p>这个命令将先执行command1命令，将其输出通过管道符号”|”传递给command2命令，然后再将input.txt文件中的内容作为command2命令的输入。</p><h6 id="将命令的输入从文件中读取"><a href="#将命令的输入从文件中读取" class="headerlink" title="将命令的输入从文件中读取"></a>将命令的输入从文件中读取</h6><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs shell">grep &quot;example&quot; &lt; input.txt<br></code></pre></td></tr></table></figure><p>这个命令将从一个名为input.txt的文件中读取输入，并使用grep命令在其中搜索包含”example”的行，并将结果输出到标准输出。</p><h6 id="高级重定向"><a href="#高级重定向" class="headerlink" title="高级重定向"></a>高级重定向</h6><ol><li><code>ls /usr /abc &gt;ok.txt</code>：将<code>ls /usr /abc</code>命令的输出重定向到文件<code>ok.txt</code>中，不会输出错误信息。</li><li><code>ls /usr /abc 1&gt;ok.txt</code>：将<code>ls /usr /abc</code>命令的标准输出重定向到文件<code>ok.txt</code>中，不会输出错误信息。</li><li><code>ls /usr /abc 2&gt;err.txt</code>：将<code>ls /usr /abc</code>命令的标准错误输出重定向到文件<code>err.txt</code>中，不会输出标准输出。</li><li><code>ls /usr /abc 1&gt;ok.txt 2&gt;err.txt</code>：将<code>ls /usr /abc</code>命令的标准输出重定向到文件<code>ok.txt</code>中，将标准错误输出重定向到文件<code>err.txt</code>中。</li><li><code>ls /usr /abc 2&gt;/dev/null</code>：将<code>ls /usr /abc</code>命令的标准错误输出重定向到<code>/dev/null</code>设备文件中，这样就不会输出错误信息。</li><li><code>ls /usr /abc 1&gt;/dev/null 2&gt;&amp;1</code>：将<code>ls /usr /abc</code>命令的标准输出重定向到<code>/dev/null</code>设备文件中，将标准错误输出重定向到标准输出中，这样既不会输出标准输出，也不会输出错误信息。</li></ol><p>在Linux系统中，数字<code>1</code>表示标准输出，数字<code>2</code>表示标准错误输出。<code>/dev/null</code>是一个特殊的设备文件，它可以将数据丢弃，相当于一个黑洞。因此，将输出重定向到<code>/dev/null</code>设备文件中，可以将输出数据丢弃。<code>2&gt;&amp;1</code>表示将标准错误输出重定向到标准输出，这样就可以将错误信息输出到与标准输出相同的位置，方便查看。</p><h5 id="管道"><a href="#管道" class="headerlink" title="管道"></a>管道</h5><p>管道是一种Shell特性，可以将一个命令的输出作为另一个命令的输入。使用管道符号|将两个命令连接在一起，第一个命令的输出将作为第二个命令的输入，可以实现多个命令的组合使用。</p><p>例子：</p><p>假设有一个文本文件input.txt，其中包含了以下内容：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs shell">apple<br>banana<br>orange<br></code></pre></td></tr></table></figure><p>现在我们想要将这个文件中的每个单词转换成大写形式，并按照字母顺序排序。我们可以使用以下命令实现：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs shell">tr &#x27;[:lower:]&#x27; &#x27;[:upper:]&#x27; &lt; input.txt | sort<br></code></pre></td></tr></table></figure><p>这个命令包含了两个命令，使用管道符号”|”将它们连接起来。第一个命令是tr命令，它将input.txt文件中的每个小写字母转换成大写字母。”&lt;”符号将input.txt文件作为tr命令的输入。</p><p>第二个命令是sort命令，它将第一个命令的输出进行字母排序。由于第一个命令的输出已经通过管道符号”|”传递给了第二个命令，因此不需要使用输入重定向符号。</p><p>最终的输出将是：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs shell">APPLE<br>BANANA<br>ORANGE<br></code></pre></td></tr></table></figure><p>这些单词按照字母顺序排序，并且每个单词都转换成了大写形式。</p><h4 id="快捷键一览"><a href="#快捷键一览" class="headerlink" title="快捷键一览"></a>快捷键一览</h4><h5 id="历史记录查看-1"><a href="#历史记录查看-1" class="headerlink" title="历史记录查看"></a>历史记录查看</h5><ul><li>键盘↑↓键 上下翻看历史输入命令</li><li>ctrl + p 前一条指令</li><li>ctrl + n 后一条指令</li><li>ctrl + r  反向搜索命令历史记录<ul><li>输入内容后，系统会找到最近一个包含这个内容的命令</li><li>找到命令后，按回车执行命令，按上下键查找该命令前后命令，按左右键移动光标并修改命令</li></ul></li></ul><h5 id="光标移动"><a href="#光标移动" class="headerlink" title="光标移动"></a>光标移动</h5><ul><li>ctrl + a ahead 移动光标到行首 </li><li>ctrl + e end 移动光标到行尾 </li><li>ctrl + b back 往后移动一个字符(行首方向) </li><li>ctrl + f front 往前移动一个字符(行尾方向) </li><li>ctrl + t 交换光标所在字符与后一个字符的位置(行首方向)</li></ul><h5 id="快速操作"><a href="#快速操作" class="headerlink" title="快速操作"></a>快速操作</h5><ul><li>ctrl + h 删除光标前一个字符(行首方向)</li><li>ctrl + d 删除光标所在字符</li><li>ctrl + w 剪切后一个单词(行首方向)</li><li>ctrl + u 剪切到行首</li><li>ctrl + k 剪切到行尾</li><li>ctrl + y 粘贴剪切</li></ul><h3 id="其他指令"><a href="#其他指令" class="headerlink" title="其他指令"></a>其他指令</h3><h4 id="文件处理类命令"><a href="#文件处理类命令" class="headerlink" title="文件处理类命令"></a>文件处理类命令</h4><h5 id="mkdir命令"><a href="#mkdir命令" class="headerlink" title="mkdir命令"></a>mkdir命令</h5><p><code>mkdir</code>是一个在Linux系统中创建目录的命令。<code>mkdir</code>命令的基本语法如下：</p><figure class="highlight arduino"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs arduino">mkdir 目录名<br></code></pre></td></tr></table></figure><p>其中，目录名表示要创建的目录名称。</p><p>例如，以下命令将在当前目录下创建一个名为<code>mydir</code>的目录：</p><figure class="highlight arduino"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs arduino">mkdir mydir<br></code></pre></td></tr></table></figure><p>如果要在当前目录下创建一个嵌套目录，可以使用<code>-p</code>选项，例如：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">mkdir</span> -p mydir/subdir<br></code></pre></td></tr></table></figure><p>该命令将同时创建一个名为<code>mydir</code>的目录和一个名为<code>subdir</code>的子目录。</p><h5 id="rmdir命令"><a href="#rmdir命令" class="headerlink" title="rmdir命令"></a>rmdir命令</h5><p>与<code>mkdir</code>命令相反的是<code>rmdir</code>命令，用于在Linux系统中删除一个空目录。<code>rmdir</code>命令的基本语法如下：</p><figure class="highlight arduino"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs arduino">rmdir 目录名<br></code></pre></td></tr></table></figure><p>其中，目录名表示要删除的目录名称。例如，以下命令将删除当前目录下的<code>mydir</code>目录：</p><figure class="highlight arduino"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs arduino">rmdir mydir<br></code></pre></td></tr></table></figure><p>需要注意的是，<code>rmdir</code>命令只能删除空目录，如果要删除非空目录，可以使用<code>rm -r</code>命令。</p><h5 id="rm命令"><a href="#rm命令" class="headerlink" title="rm命令"></a>rm命令</h5><p><code>rm</code>是Linux&#x2F;Unix中一个非常重要的命令，用于删除文件和目录。<code>rm</code>命令可以删除文件、目录以及它们的子目录和文件。<code>rm</code>命令的基本语法如下：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">rm</span> [选项] 文件/目录<br></code></pre></td></tr></table></figure><p>其中，文件&#x2F;目录表示要删除的文件或目录，选项可用于指定不同的删除方式。常用的选项包括：</p><ul><li><code>-f</code>：强制删除文件或目录，不提示用户确认；</li><li><code>-i</code>：交互式删除，删除前提示用户确认；</li><li><code>-r</code>：递归删除目录及其下所有的子目录和文件；</li><li><code>-v</code>：显示详细信息，显示删除的文件名。</li></ul><p>例如，以下命令将删除文件example.txt：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">rm</span> example.txt<br></code></pre></td></tr></table></figure><p>如果想删除目录example_dir以及它下面的所有子目录和文件，可以使用<code>-r</code>选项：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">rm</span> -r example_dir<br></code></pre></td></tr></table></figure><p>如果不想强制删除文件&#x2F;目录，可以使用<code>-i</code>选项：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">rm</span> -i example.txt<br></code></pre></td></tr></table></figure><p><code>rm</code>命令在Linux系统中非常常用，但要特别注意在使用<code>-r</code>选项时要非常小心，确保不会误删重要文件或目录。</p><p><code>rm</code>和<code>rmdir</code>命令都是Linux系统中用于删除文件和目录的命令，但它们之间有一些区别。</p><ul><li><p><code>rm</code>命令用于删除一个或多个文件，它的语法为：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">rm</span> [选项] 文件名<br></code></pre></td></tr></table></figure><p>其中，选项可用于指定删除操作的方式，如<code>-r</code>选项用于删除目录及其内容，<code>-f</code>选项用于强制删除文件，而不提示用户确认操作。</p></li><li><p><code>rmdir</code>命令用于删除空目录，它的语法为：</p><figure class="highlight arduino"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs arduino">rmdir [选项] 目录名<br></code></pre></td></tr></table></figure><p>其中，选项可用于指定删除操作的方式，如<code>-p</code>选项用于递归删除目录及其空父目录，<code>-v</code>选项用于显示删除操作的详细信息。</p></li></ul><p>可以看出，<code>rm</code>命令和<code>rmdir</code>命令的主要区别在于，<code>rm</code>命令可以删除文件和非空目录，而<code>rmdir</code>命令只能删除空目录。</p><p>需要注意的是，使用<code>rm</code>命令和<code>rmdir</code>命令时要非常谨慎，因为它们的操作是不可逆的。建议在删除文件或目录之前先确认要删除的内容，并备份重要数据，以免误删数据导致数据丢失。</p><h5 id="touch命令"><a href="#touch命令" class="headerlink" title="touch命令"></a>touch命令</h5><p><code>touch</code>是一个用于创建空文件或更改文件时间戳的命令行工具。它的基本语法如下：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">touch</span> [选项] 文件名<br></code></pre></td></tr></table></figure><p>其中，文件名表示要创建或更改时间戳的文件名，选项用于指定不同的操作。常用的选项包括：</p><ul><li><code>-a</code>：只更改文件的访问时间；</li><li><code>-m</code>：只更改文件的修改时间；</li><li><code>-c</code>：仅在文件不存在时才创建文件；</li><li><code>-t</code>：使用指定的时间戳更新文件的时间。</li></ul><p>例如，以下命令将创建一个名为example.txt的空文件：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">touch</span> example.txt<br></code></pre></td></tr></table></figure><p>如果文件已经存在，则<code>touch</code>命令将更新文件的访问和修改时间。要仅更改文件的访问或修改时间，可以使用<code>-a</code>或<code>-m</code>选项：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">touch</span> -a example.txt  <span class="hljs-comment"># 仅更改访问时间</span><br><span class="hljs-built_in">touch</span> -m example.txt  <span class="hljs-comment"># 仅更改修改时间</span><br></code></pre></td></tr></table></figure><p>要指定要创建或修改的时间戳，可以使用<code>-t</code>选项：</p><figure class="highlight apache"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs apache"><span class="hljs-attribute">touch</span> -t <span class="hljs-number">202201010101</span> example.txt  # 将文件的时间戳设置为<span class="hljs-number">2022</span>年<span class="hljs-number">1</span>月<span class="hljs-number">1</span>日<span class="hljs-number">1</span>点<span class="hljs-number">01</span>分<br></code></pre></td></tr></table></figure><p><code>touch</code>命令在Linux系统中非常常用，可以用于创建空文件或更改文件时间戳。在一些情况下，例如编写Makefile时，还可以用于强制更新文件的时间戳以触发编译或其他操作。</p><h5 id="cp命令"><a href="#cp命令" class="headerlink" title="cp命令"></a>cp命令</h5><p><code>cp</code>命令是一个在Linux和其他Unix系统上使用的命令，用于将一个或多个文件或目录从一个位置复制到另一个位置。它的基本语法如下：</p><figure class="highlight avrasm"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs avrasm"><span class="hljs-keyword">cp</span> [选项] 源文件 目标文件<br></code></pre></td></tr></table></figure><p>其中，<code>源文件</code>表示要复制的文件或目录，<code>目标文件</code>表示要将文件或目录复制到的位置。常用的选项包括：</p><ul><li><code>-r</code>：递归地复制目录及其内容；</li><li><code>-i</code>：在复制时显示提示，以防止覆盖已存在的文件；</li><li><code>-v</code>：在复制时显示详细的输出信息。</li></ul><p>例如，以下命令将文件<code>file1.txt</code>复制到目录<code>/home/user1</code>：</p><figure class="highlight awk"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs awk">cp file1.txt <span class="hljs-regexp">/home/u</span>ser1<br></code></pre></td></tr></table></figure><p>如果要将目录<code>dir1</code>及其内容复制到目录<code>dir2</code>中，可以使用<code>-r</code>选项进行递归复制：</p><figure class="highlight avrasm"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs avrasm"><span class="hljs-keyword">cp</span> -r dir1 dir2<br></code></pre></td></tr></table></figure><p>使用<code>-i</code>选项可以在复制时显示提示：</p><figure class="highlight awk"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs awk">cp -i file1.txt <span class="hljs-regexp">/home/u</span>ser1<br></code></pre></td></tr></table></figure><p>使用<code>-v</code>选项可以显示详细的输出信息：</p><figure class="highlight awk"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs awk">cp -v file1.txt <span class="hljs-regexp">/home/u</span>ser1<br></code></pre></td></tr></table></figure><p><code>cp</code>命令在Linux系统中非常常用，可以用于在文件系统中复制文件和目录。</p><h5 id="mv命令"><a href="#mv命令" class="headerlink" title="mv命令"></a>mv命令</h5><p><code>mv</code>命令是Linux系统中的一个常用命令，用于移动或重命名文件和目录。<code>mv</code>命令的语法如下：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">mv</span> [选项] 源文件或目录 目标文件或目录<br></code></pre></td></tr></table></figure><p>其中，<code>源文件或目录</code>表示要移动或重命名的文件或目录，<code>目标文件或目录</code>表示移动或重命名后的目标文件或目录。如果目标文件或目录已经存在，<code>mv</code>命令将覆盖它。</p><p>常用的选项包括：</p><ul><li><code>-i</code>：在移动或重命名文件时，如果目标文件已经存在，则提示用户是否覆盖它；</li><li><code>-f</code>：在移动或重命名文件时，强制覆盖目标文件，不询问用户。</li></ul><p>例如，以下命令将文件<code>file.txt</code>移动到目录<code>/tmp</code>下：</p><figure class="highlight gradle"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs gradle">mv <span class="hljs-keyword">file</span>.txt <span class="hljs-regexp">/tmp/</span><br></code></pre></td></tr></table></figure><p>如果想将文件<code>file.txt</code>重命名为<code>newfile.txt</code>，可以使用以下命令：</p><figure class="highlight haxe"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs haxe">mv file.txt <span class="hljs-keyword">new</span><span class="hljs-type">file</span>.txt<br></code></pre></td></tr></table></figure><p>如果想在移动文件时询问用户是否覆盖目标文件，可以使用<code>-i</code>选项：</p><figure class="highlight gradle"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs gradle">mv -i <span class="hljs-keyword">file</span>.txt <span class="hljs-regexp">/tmp/</span><br></code></pre></td></tr></table></figure><p>如果想在移动文件时强制覆盖目标文件，可以使用<code>-f</code>选项：</p><figure class="highlight gradle"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs gradle">mv -f <span class="hljs-keyword">file</span>.txt <span class="hljs-regexp">/tmp/</span><br></code></pre></td></tr></table></figure><p><code>mv</code>命令还可以用于移动和重命名目录。例如，以下命令将目录<code>/usr/local/bin</code>移动到目录<code>/opt</code>下：</p><figure class="highlight awk"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs awk">mv <span class="hljs-regexp">/usr/</span>local<span class="hljs-regexp">/bin /</span>opt/<br></code></pre></td></tr></table></figure><p>如果想将目录<code>/usr/local/bin</code>重命名为<code>/usr/local/bin_old</code>，可以使用以下命令：</p><figure class="highlight awk"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs awk">mv <span class="hljs-regexp">/usr/</span>local<span class="hljs-regexp">/bin /u</span>sr<span class="hljs-regexp">/local/</span>bin_old<br></code></pre></td></tr></table></figure><p>总之，<code>mv</code>命令是Linux系统中非常常用的命令之一，可以用于移动、重命名文件和目录等操作。</p><h4 id="文件查看类命令"><a href="#文件查看类命令" class="headerlink" title="文件查看类命令"></a>文件查看类命令</h4><h5 id="cat命令"><a href="#cat命令" class="headerlink" title="cat命令"></a>cat命令</h5><p><code>cat</code>是一个常用的Linux命令，用于在终端上查看和连接文件内容。它的基本语法如下：</p><figure class="highlight css"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs css">cat <span class="hljs-selector-attr">[选项]</span> <span class="hljs-selector-attr">[文件名]</span><br></code></pre></td></tr></table></figure><p>其中，文件名表示要查看和连接的文件名，选项可用于指定不同的操作方式。常用的选项包括：</p><ul><li><code>-n</code>：在每一行前面加上行号；</li><li><code>-E</code>：在每一行的结尾添加一个美元符号（$）；</li><li><code>-T</code>：将制表符（Tab）替换为^I字符；</li><li><code>-s</code>：将连续多个空行压缩为一个空行。</li></ul><p>例如，以下命令将显示文件example.txt的内容：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">cat</span> example.txt<br></code></pre></td></tr></table></figure><p>如果想在每一行前面加上行号，可以使用<code>-n</code>选项：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">cat</span> -n example.txt<br></code></pre></td></tr></table></figure><p>如果想在每一行的结尾添加一个美元符号，可以使用<code>-E</code>选项：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">cat</span> -E example.txt<br></code></pre></td></tr></table></figure><p>如果想将制表符替换为^I字符，可以使用<code>-T</code>选项：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">cat</span> -T example.txt<br></code></pre></td></tr></table></figure><p>如果想将连续多个空行压缩为一个空行，可以使用<code>-s</code>选项：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">cat</span> -s example.txt<br></code></pre></td></tr></table></figure><p>除了查看文件内容外，<code>cat</code>命令还可以用于连接多个文件。例如，以下命令将连接文件example1.txt和example2.txt的内容，并将结果输出到文件result.txt中：</p><figure class="highlight stylus"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs stylus">cat example1<span class="hljs-selector-class">.txt</span> example2<span class="hljs-selector-class">.txt</span> &gt; result.txt<br></code></pre></td></tr></table></figure><p><code>cat</code>命令在Linux系统中非常常用，是一个十分实用的命令。</p><h5 id="more命令"><a href="#more命令" class="headerlink" title="more命令"></a>more命令</h5><p><code>more</code>命令是一个基于终端的分页程序，用于查看大型文本文件的内容。它允许用户按页或按行浏览文件内容，同时还支持向前&#x2F;向后翻页、搜索和跳转等功能。</p><p><code>more</code>命令的基本语法如下：</p><figure class="highlight css"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs css">more <span class="hljs-selector-attr">[选项]</span> 文件名<br></code></pre></td></tr></table></figure><p>其中，文件名表示要查看的文件名，选项可用于指定不同的操作方式。常用的选项包括：</p><ul><li><code>-d</code>：显示每页的百分比和当前行号；</li><li><code>-l</code>：使用长列表格式，显示每行的行号和制表符等细节信息；</li><li><code>-p</code>：指定分页符，格式为“^字符”；</li><li><code>-s</code>：抑制输出，只显示每页的页码信息；</li><li><code>-u</code>：将所有字符转换为小写字母。</li></ul><p>例如，以下命令将按页查看文件example.txt的内容：</p><figure class="highlight dos"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs dos"><span class="hljs-built_in">more</span> example.txt<br></code></pre></td></tr></table></figure><p>按空格键可以向下翻页，按Enter键可以向下滚动一行，按q键可以退出查看模式。</p><p><code>more</code>命令在Unix&#x2F;Linux系统中广泛使用，尤其在处理大型文本文件时非常方便。不过，由于其交互式的特性，<code>more</code>命令在批处理和脚本中的应用不如其他命令（如<code>cat</code>和<code>less</code>）那样方便。</p><h5 id="less命令"><a href="#less命令" class="headerlink" title="less命令"></a>less命令</h5><p><code>less</code>是Linux&#x2F;Unix系统中的一个文件查看器，可以用来浏览文本文件，比<code>more</code>命令更加强大，支持向前和向后翻页、搜索等功能。与<code>more</code>命令不同的是，<code>less</code>命令可以向前或向后滚动屏幕，而<code>more</code>命令只能向前滚动。</p><p><code>less</code>命令的基本语法为：</p><figure class="highlight cmake"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs cmake"><span class="hljs-keyword">less</span> [选项] 文件名<br></code></pre></td></tr></table></figure><p>其中，文件名表示要查看的文件，选项包括：</p><ul><li><code>-N</code>：显示行号；</li><li><code>-n</code>：不显示行号；</li><li><code>-i</code>：搜索时忽略大小写；</li><li><code>-I</code>：搜索时区分大小写；</li><li><code>/pattern</code>：搜索指定的模式；</li><li><code>q</code>：退出<code>less</code>命令；</li><li><code>h</code>：显示帮助信息。</li></ul><p>使用<code>less</code>命令时，可以通过上下箭头键向前或向后滚动屏幕，也可以使用<code>PageUp</code>和<code>PageDown</code>键翻页。在查看文件时，可以按下<code>/</code>键进行搜索，输入要搜索的内容，然后按下<code>Enter</code>键。在搜索结果中，可以按下<code>n</code>键向后查找匹配项，按下<code>N</code>键向前查找匹配项。</p><p>举个例子，如果要查看文件<code>example.txt</code>，可以使用以下命令：</p><figure class="highlight cmake"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs cmake"><span class="hljs-keyword">less</span> example.txt<br></code></pre></td></tr></table></figure><p>这将打开<code>less</code>命令的查看器，并显示文件的第一页内容。可以使用上下箭头键向前或向后滚动屏幕，使用<code>q</code>键退出<code>less</code>命令。如果要搜索文件中的内容，可以按下<code>/</code>键输入要搜索的内容，然后按下<code>Enter</code>键。</p><h5 id="head命令"><a href="#head命令" class="headerlink" title="head命令"></a>head命令</h5><p><code>head</code>命令是Linux&#x2F;Unix系统中的一个命令行工具，用于显示文件的开头若干行，默认显示文件的前10行。</p><p><code>head</code>命令的基本语法为：</p><figure class="highlight css"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs css">head <span class="hljs-selector-attr">[选项]</span> <span class="hljs-selector-attr">[文件名]</span><br></code></pre></td></tr></table></figure><p>其中，<code>选项</code>可选，常用的选项包括：</p><ul><li><code>-n</code>：指定要显示的行数，例如<code>-n 20</code>表示显示前20行，默认为10行；</li><li><code>-v</code>：显示文件名，当同时处理多个文件时，使用该选项可以在每个文件的开头显示文件名。</li></ul><p><code>文件名</code>表示要显示的文件名，可以同时处理多个文件，例如：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">head</span> file1 file2 file3<br></code></pre></td></tr></table></figure><p>如果没有指定文件名，则<code>head</code>命令从标准输入读取数据。</p><p>以下是一些示例：</p><ol><li><p>显示文件<code>example.txt</code>的前10行：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">head</span> example.txt<br></code></pre></td></tr></table></figure></li><li><p>显示文件<code>example.txt</code>的前20行：</p><figure class="highlight apache"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs apache"><span class="hljs-attribute">head</span> -n <span class="hljs-number">20</span> example.txt<br></code></pre></td></tr></table></figure></li><li><p>显示文件<code>example.txt</code>和<code>example2.txt</code>的前10行：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">head</span> example.txt example2.txt<br></code></pre></td></tr></table></figure></li><li><p>显示文件<code>example.txt</code>的前10行，并显示文件名：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">head</span> -v example.txt<br></code></pre></td></tr></table></figure></li></ol><p><code>head</code>命令在处理文本文件时非常有用，特别是当需要查看文件开头的内容时。同时，它还可以与其他命令一起使用，例如使用管道符号将<code>head</code>命令的输出作为其他命令的输入。</p><h5 id="tail命令"><a href="#tail命令" class="headerlink" title="tail命令"></a>tail命令</h5><p><code>tail</code> 命令在 Linux 中用于显示文件的末尾内容。默认情况下，它显示文件的最后 10 行。您还可以指定要显示的行数和文件名。</p><p><code>tail</code> 命令的基本语法如下：</p><figure class="highlight css"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs css">tail <span class="hljs-selector-attr">[选项]</span> <span class="hljs-selector-attr">[文件名]</span><br></code></pre></td></tr></table></figure><p>其中，选项用于指定一些参数，例如 <code>-n</code> 用于指定要显示的行数，而文件名则是要显示其末尾内容的文件名。</p><p>以下是一些常见的选项：</p><ul><li><code>-f</code>：显示文件的末尾内容，并在文件更新时自动刷新。</li><li><code>-n</code>：指定要显示的行数。例如，<code>-n 20</code> 表示显示文件的最后 20 行。</li><li><code>-q</code>：当显示多个文件时，只显示文件名而不显示文件头部。</li><li><code>-v</code>：当显示多个文件时，显示文件名以及文件头部。</li></ul><p>以下是一些示例：</p><ol><li>显示文件的最后 10 行：</li></ol><figure class="highlight applescript"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs applescript">tail <span class="hljs-built_in">file</span>.txt<br></code></pre></td></tr></table></figure><ol start="2"><li>显示文件的最后 20 行：</li></ol><figure class="highlight apache"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs apache"><span class="hljs-attribute">tail</span> -n <span class="hljs-number">20</span> file.txt<br></code></pre></td></tr></table></figure><ol start="3"><li>显示多个文件的末尾内容：</li></ol><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">tail</span> file1.txt file2.txt<br></code></pre></td></tr></table></figure><ol start="4"><li>在文件更新时自动刷新并显示其末尾内容：</li></ol><figure class="highlight applescript"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs applescript">tail -f <span class="hljs-built_in">file</span>.txt<br></code></pre></td></tr></table></figure><p><code>tail</code> 命令通常用于查看正在进行的日志文件，因为它可以在文件更新时自动刷新并显示新的内容。</p><h4 id="wc命令"><a href="#wc命令" class="headerlink" title="wc命令"></a>wc命令</h4><p>在Linux中，<code>wc</code>是一个命令行工具，用于计算文件中的行数、单词数和字符数。该命令的名称代表“word count”（单词计数），但它可以用于计算文本文件中的其他信息，例如字节数、行数、段落数等。</p><p><code>wc</code>命令的基本语法如下：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">wc</span> [选项] 文件名<br></code></pre></td></tr></table></figure><p>其中，文件名表示要计算信息的文件名，选项可用于指定不同的计数方法。常用的选项包括：</p><ul><li><code>-l</code>：计算文件中的行数；</li><li><code>-w</code>：计算文件中的单词数；</li><li><code>-c</code>：计算文件中的字符数；</li><li><code>-m</code>：计算文件中的字符数（包括多字节字符）；</li><li><code>-L</code>：计算文件中最长行的长度。</li></ul><p>例如，以下命令将计算文件example.txt中的行数、单词数和字符数：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs shell">wc example.txt<br></code></pre></td></tr></table></figure><p>如果想只计算行数，可以使用<code>-l</code>选项：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs shell">wc -l example.txt<br></code></pre></td></tr></table></figure><p>如果想同时计算行数和单词数，可以使用<code>-lw</code>选项：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs shell">wc -lw example.txt<br></code></pre></td></tr></table></figure><p><code>wc</code>命令在Linux系统中非常常用，可以用于计算文本文件中的各种信息，特别是在处理大量文本文件时非常方便。</p><h4 id="echo命令"><a href="#echo命令" class="headerlink" title="echo命令"></a>echo命令</h4><p><code>echo</code>命令是一个Linux&#x2F;Unix命令行工具，用于在标准输出上打印文本字符串。该命令的基本语法如下：</p><figure class="highlight css"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs css">echo <span class="hljs-selector-attr">[选项]</span> <span class="hljs-selector-attr">[字符串]</span><br></code></pre></td></tr></table></figure><p>其中，字符串表示要输出的文本，选项可用于控制输出格式或处理特殊字符。常用的选项包括：</p><ul><li><code>-n</code>：在输出末尾不添加换行符；</li><li><code>-e</code>：允许使用特殊字符，例如<code>\n</code>表示换行符、<code>\t</code>表示制表符等。</li></ul><p>例如，以下命令将在标准输出上打印字符串”Hello World!”：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs shell">echo &quot;Hello World!&quot;<br></code></pre></td></tr></table></figure><p>如果想在输出末尾不添加换行符，可以使用<code>-n</code>选项：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs shell">echo -n &quot;Hello World!&quot;<br></code></pre></td></tr></table></figure><p>如果想在输出中添加特殊字符，可以使用<code>-e</code>选项：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs shell">echo -e &quot;Hello\tWorld!\n&quot;<br></code></pre></td></tr></table></figure><p><code>echo</code>命令在Linux系统中非常常用，可以用于打印文本、输出变量、生成脚本等。</p><h4 id="ls命令"><a href="#ls命令" class="headerlink" title="ls命令"></a>ls命令</h4><p>显示指定工作目录下的内容</p><p><code>ls</code>是Linux中的一个命令，用于列出目录中的文件和子目录。在默认情况下，<code>ls</code>列出当前工作目录中的文件和子目录，并将它们按字母顺序排序。如果指定了文件名或目录名作为参数，则<code>ls</code>会列出指定的文件或目录。</p><p><code>ls</code>命令的基本语法如下：</p><figure class="highlight css"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs css">ls <span class="hljs-selector-attr">[选项]</span> <span class="hljs-selector-attr">[文件名/目录名]</span><br></code></pre></td></tr></table></figure><table><thead><tr><th>option</th><th>描述</th></tr></thead><tbody><tr><td>-a</td><td>all,  列出所有文件，包含隐藏文件</td></tr><tr><td>-l</td><td>long,  长格式打印</td></tr><tr><td>-i</td><td>inode,  列出inode节点的值</td></tr><tr><td>-t</td><td>time,  按时间排序</td></tr><tr><td>-S</td><td>size,  按文件大小排序</td></tr><tr><td>-d</td><td>directory,  只显示目录，不显示目录下的内容</td></tr><tr><td>-R</td><td>recursive,  递归显示目录及子目录的内容</td></tr></tbody></table><p>例如，以下命令将列出当前工作目录中的文件和子目录：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs shell">ls<br></code></pre></td></tr></table></figure><p>如果要列出当前工作目录中所有文件和子目录，包括隐藏文件，可以使用<code>-a</code>选项：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs shell">ls -a<br></code></pre></td></tr></table></figure><p>如果要列出当前工作目录中所有文件和子目录的详细信息，包括文件的权限、所有者、文件大小、修改时间等，可以使用<code>-l</code>选项：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs shell">ls -l<br></code></pre></td></tr></table></figure><p>如果要列出指定目录中的所有文件和子目录，可以将目录名作为参数传递给<code>ls</code>命令：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs shell">ls /usr/bin<br></code></pre></td></tr></table></figure><p><code>ls</code>命令在Linux系统中非常常用，可以用于查看文件和目录的详细信息，以及快速定位文件和目录的位置。</p><p><img src="/images/linux006.jpg"></p><h4 id="pwd命令"><a href="#pwd命令" class="headerlink" title="pwd命令"></a>pwd命令</h4><p><code>pwd</code>命令是Linux&#x2F;Unix操作系统中的一个命令，它的含义是“print working directory”，即“显示当前工作目录”。该命令用于显示当前工作目录的完整路径名，即当前用户所处的目录的路径。</p><p>在Linux&#x2F;Unix操作系统中，用户可以在不同的目录中进行文件操作。例如，可以在&#x2F;home&#x2F;user&#x2F;Documents目录中创建、编辑、复制和删除文件，而在&#x2F;home&#x2F;user&#x2F;Pictures目录中查看、保存和删除图片文件。为了知道当前用户所处的目录是哪个，可以使用<code>pwd</code>命令。</p><p><code>pwd</code>命令的使用方法很简单，只需在终端中输入<code>pwd</code>，然后按Enter键即可。系统将返回当前工作目录的完整路径名，例如：</p><figure class="highlight awk"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs awk">$ pwd<br><span class="hljs-regexp">/home/u</span>ser/Documents<br></code></pre></td></tr></table></figure><p>在上面的例子中，<code>pwd</code>命令显示当前用户所处的目录是&#x2F;home&#x2F;user&#x2F;Documents。</p><p><code>pwd</code>命令可以与其他命令一起使用，例如可以使用<code>cd</code>命令更改当前工作目录，然后使用<code>pwd</code>命令确认更改是否生效。例如：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs shell"><span class="hljs-meta prompt_">$ </span><span class="language-bash"><span class="hljs-built_in">cd</span> /home/user/Pictures</span><br><span class="hljs-meta prompt_">$ </span><span class="language-bash"><span class="hljs-built_in">pwd</span></span><br>/home/user/Pictures<br></code></pre></td></tr></table></figure><p>在上面的例子中，用户首先使用<code>cd</code>命令更改当前工作目录为&#x2F;home&#x2F;user&#x2F;Pictures，然后使用<code>pwd</code>命令确认更改是否生效。系统返回的结果是当前工作目录为&#x2F;home&#x2F;user&#x2F;Pictures。</p><p>总之，<code>pwd</code>命令是一个非常简单但有用的命令，它可以帮助用户确认当前所处的工作目录，方便进行文件操作和其他命令的使用。</p><h4 id="cd命令"><a href="#cd命令" class="headerlink" title="cd命令"></a>cd命令</h4><p><code>cd</code>是Linux&#x2F;Unix中的一个命令，用于更改当前工作目录。它是“change directory”的缩写。</p><p><code>cd</code>命令的基本语法如下：</p><figure class="highlight apache"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs apache"><span class="hljs-attribute">cd</span><span class="hljs-meta"> [目录路径]</span><br></code></pre></td></tr></table></figure><p>其中，目录路径表示要切换到的目录的路径。如果没有指定目录路径，则<code>cd</code>命令将切换到当前用户的主目录。</p><p>以下是一些常用的<code>cd</code>命令的示例：</p><ul><li><code>cd</code>：切换到当前用户的主目录。</li><li><code>cd ~</code>：切换到当前用户的主目录（与上面的命令等效）。</li><li><code>cd /</code>：切换到根目录。</li><li><code>cd ..</code>：切换到上一级目录。</li><li><code>cd ../..</code>：切换到上两级目录。</li><li><code>cd /path/to/directory</code>：切换到指定路径的目录。</li></ul><p>使用<code>cd</code>命令可以快速切换当前工作目录，方便用户在不同目录下进行操作。</p><h2 id="第四讲"><a href="#第四讲" class="headerlink" title="第四讲"></a>第四讲</h2><h3 id="用户类型"><a href="#用户类型" class="headerlink" title="用户类型"></a>用户类型</h3><h4 id="超级用户"><a href="#超级用户" class="headerlink" title="超级用户"></a>超级用户</h4><p>root，根用户，类似Windows中的超级管理员账户，有最高的权限，可以对Linux做任何操作 </p><h4 id="普通用户"><a href="#普通用户" class="headerlink" title="普通用户"></a>普通用户</h4><p>受限的权限，普通用户没有对系统的完全控制权，而且用户之间私人的资源是相互隔离的 </p><h4 id="系统用户"><a href="#系统用户" class="headerlink" title="系统用户"></a>系统用户</h4><ul><li>与系统和程序服务相关的用户 </li><li>默认情况下，这些特殊用户是无法登录的，如果给这些用户授权登陆口令后，就可以使用这些用户登录系统 </li><li>&#x2F;sbin&#x2F;nologin 无法登陆shell</li><li>举个例子来说，如果你安装了一个数据库服务，通常会创建一个专门的系统用户来运行这个服务，这个用户没有登录系统的权限，也没有终端Shell，只能执行与这个服务相关的操作。这个用户通常拥有较低的权限，只能读取、写入和修改数据库中的数据，不能执行其他的操作。这样做的好处是，即使攻击者通过某些漏洞获取了这个服务的权限，也无法通过这个用户的权限直接访问系统和其他的服务，从而保护了系统的安全性。</li></ul><h3 id="用户信息文件"><a href="#用户信息文件" class="headerlink" title="用户信息文件"></a>用户信息文件</h3><p>&#x2F;etc&#x2F;passwd </p><ul><li>每一行存储一个用户的账号信息</li><li>用户名:加密密码:用户ID:用户组ID:用户信息:用户主目录:登录Shell</li><li>超级用户root的UID是0</li><li>系统用户的UID在1000以内</li><li>普通用户的UID从1000开始往上编号</li></ul><h3 id="口令文件"><a href="#口令文件" class="headerlink" title="口令文件"></a>口令文件</h3><p>&#x2F;etc&#x2F;shadow</p><ul><li>每一行存储一个用户的登录密码信息，加密</li><li>只有root用户才能读取这个文件(怎么知道？)</li><li>用户名:加密口令:上次更新时间:允许更改时间:要求更改时间:取消口令之前的警告时间:取消和停用之间的时间:账户口令的天数:账户终止时间:特殊标志</li></ul><h3 id="组信息文件"><a href="#组信息文件" class="headerlink" title="组信息文件"></a>组信息文件</h3><p>&#x2F;etc&#x2F;group</p><ul><li>每一行记录系统中的用户组信息 </li><li>组名:密码字段:用户组ID :用户名列表 </li><li>用户名列表用冒号分隔多个用户名 </li><li>默认情况下，创建用户的时候，系统会自动创建一个同名的组，作为该用户的主组。</li></ul><h3 id="组信息类命令"><a href="#组信息类命令" class="headerlink" title="组信息类命令"></a>组信息类命令</h3><h4 id="useradd命令"><a href="#useradd命令" class="headerlink" title="useradd命令"></a>useradd命令</h4><p>作用：新增用户</p><p>格式： useradd [选项] 用户名</p><p>root用户</p><table><thead><tr><th align="left">选项</th><th>描述</th></tr></thead><tbody><tr><td align="left">-d  dirName</td><td>指定用户主目录。默认将会在&#x2F;home目录下新建一个与用户名相同的用户主目录</td></tr><tr><td align="left">-s  shellName</td><td>指定用户登录时使用的shell。默认为&#x2F;bin&#x2F;sh</td></tr><tr><td align="left">-g  groupName</td><td>指定用户所属的组名。创建用户时默认创建一个与用户名同名的组</td></tr><tr><td align="left">-G  groupList</td><td>一个用户可以属于一个组，也可以属于多个组。用户创建时属于的组称为主组，其他的叫附属组。</td></tr><tr><td align="left">-u  uid</td><td>指定新用户的UID。新用户默认UID为当前最高UID值+1</td></tr></tbody></table><h4 id="passwd命令"><a href="#passwd命令" class="headerlink" title="passwd命令"></a>passwd命令</h4><p>作用：修改密码<br>格式：passwd [用户名]<br>普通用户只能修改自己密码root可以修改其他人<br>高级应用-无交互设置密码</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs shell">echo xxx | passwd --stdin root<br></code></pre></td></tr></table></figure><h4 id="usermod命令"><a href="#usermod命令" class="headerlink" title="usermod命令"></a>usermod命令</h4><p>作用：修改用户属性<br>格式：usermod  [选项] 用户名<br>root权限</p><table><thead><tr><th>选项</th><th>描述</th></tr></thead><tbody><tr><td>-d  dirName</td><td>指定用户主目录。默认将会在&#x2F;home目录下新建一个与用户名相同的用户主目录</td></tr><tr><td>-s  shellName</td><td>指定用户登录时使用的shell。默认为&#x2F;bin&#x2F;sh</td></tr><tr><td>-g  groupName</td><td>指定用户所属的组名。创建用户时默认创建一个与用户名同名的组</td></tr><tr><td>-G  groupList</td><td>一个用户可以属于一个组，也可以属于多个组。用户创建时属于的组称为主组，其他的叫附属组。</td></tr><tr><td>-u  uid</td><td>指定新用户的UID。新用户默认UID为当前最高UID值+1</td></tr></tbody></table><h4 id="userdel命令"><a href="#userdel命令" class="headerlink" title="userdel命令"></a>userdel命令</h4><p>作用：删除用户<br>格式：userdel  [-r] 用户名 -r 删除与用户相关的文件和目录,如用户家目录,日志,邮箱等<br>root权限</p><h4 id="groupadd命令"><a href="#groupadd命令" class="headerlink" title="groupadd命令"></a>groupadd命令</h4><p>作用：增加用户组<br>格式：groupadd [-g] 组名-g 设置GID<br>root权限</p><h4 id="groupdel命令"><a href="#groupdel命令" class="headerlink" title="groupdel命令"></a>groupdel命令</h4><p>作用：删除用户组<br>格式：groupdel  组名<br>root权限</p><h3 id="文件属性类命令"><a href="#文件属性类命令" class="headerlink" title="文件属性类命令"></a>文件属性类命令</h3><h4 id="chmod命令"><a href="#chmod命令" class="headerlink" title="chmod命令"></a>chmod命令</h4><p>作用：改变指定目录或文件的权限<br>格式：chmod [-R] 权限模式 路径-R：递归修改</p><p><img src="/images/linux007.jpg"></p><p>•u 表示该文件的拥有者</p><p>•g 表示同组用户</p><p>•o 表示其他以外的人</p><p>•a 表示这三者皆是</p><p>•+ 表示增加权限</p><p>•- 表示取消权限</p><p>•&#x3D; 表示唯一设定权限</p><p>•r 表示可读取</p><p>•w 表示可写入</p><p>•x 表示可执行</p><p>例子</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs shell"><span class="hljs-meta prompt_"># </span><span class="language-bash">该命令将文件a.txt的权限设置为所有用户都有读、写、执行的权限，但用户自身没有执行的权限，用户组没有写和执行的权限，其他用户没有任何权限。</span><br>chmod a=rwx,u-x,g-wx,o-rwx a.txt<br><span class="hljs-meta prompt_"># </span><span class="language-bash">该命令将文件a.txt的权限设置为用户具有读写权限，用户组具有读权限，其他用户没有任何权限。</span><br>chmod 640 a.txt<br><span class="hljs-meta prompt_"># </span><span class="language-bash">该命令将目录dir1及其子目录中的所有文件的权限设置为用户具有读、写、执行的权限，用户组具有读和执行的权限，其他用户具有写和执行的权限。</span><br>chmod -R 765 dir1<br></code></pre></td></tr></table></figure><p>需要注意的是，<code>chmod</code>命令中的权限表示方法有两种：一种是数字表示法，即将权限用数字表示出来，如<code>rwx</code>用<code>111</code>表示，<code>rw-</code>用<code>110</code>表示；另一种是符号表示法，即用<code>u</code>表示用户，<code>g</code>表示用户组，<code>o</code>表示其他用户，<code>a</code>表示所有用户，<code>+</code>表示增加权限，<code>-</code>表示去掉权限，<code>=</code>表示设置权限。</p><h4 id="chown命令"><a href="#chown命令" class="headerlink" title="chown命令"></a>chown命令</h4><p>作用：改变指定目录或文件的所属用户、所属组<br>格式：chown [-R] 用户名[:组名]  路径<br>root权限</p><p>例子</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs shell"><span class="hljs-meta prompt_"># </span><span class="language-bash">将文件a.txt的所有者改为wilson。</span><br>chown wilson a.txt<br><span class="hljs-meta prompt_"># </span><span class="language-bash">将文件a.txt的所有者改为wilson，同时将其所属的组也改为wilson。</span><br>chown wilson:wilson a.txt<br><span class="hljs-meta prompt_"># </span><span class="language-bash">将目录dir1及其子目录下所有文件的所有者都改为wilson。-R选项表示递归操作。</span><br>chown -R wilson dir1<br></code></pre></td></tr></table></figure><h4 id="chgrp命令"><a href="#chgrp命令" class="headerlink" title="chgrp命令"></a>chgrp命令</h4><p>作用：改变指定目录或文件的所属组。<br>格式：chgrp [-R] 组名 路径<br>root权限</p><p>例子</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs shell"><span class="hljs-meta prompt_"># </span><span class="language-bash">将文件a.txt所属的组改为wilson。</span><br>chgrp wilson a.txt<br><span class="hljs-meta prompt_"># </span><span class="language-bash">将目录dir1及其子目录下所有文件的所属组都改为wilson。-R选项表示递归操作。</span><br>chgrp -R wilson dir1<br></code></pre></td></tr></table></figure><h4 id="In命令"><a href="#In命令" class="headerlink" title="In命令"></a>In命令</h4><p>作用：创建文件的快捷方式 </p><p>格式： ln [-s] 源路径 目标路径 </p><p>硬链接(hard link) 给文件一个副本（别名），同时建立两者之间的连接关系，修改其中一个，与其连接的文件同时被修改，如果删除其中一个，其余的文件不受影响。磁盘上只有一份数据。硬链接是存在同一个文件系统中 </p><p>软链接(symbolic link) 软链接的方式则是产生一个特殊的文件，该文件的内容是指向另一个文件的位置。它只是一个快捷方式，删除了源文件，这个连接文件就没用了。软链接可以跨越不同的文件系统</p><p>例子</p><p>创建硬链接：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs shell"><span class="hljs-meta prompt_">$ </span><span class="language-bash"><span class="hljs-built_in">ln</span> source_file hardlink_file</span><br></code></pre></td></tr></table></figure><p>创建软链接：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs shell"><span class="hljs-meta prompt_">$ </span><span class="language-bash"><span class="hljs-built_in">ln</span> -s source_file softlink_file</span><br></code></pre></td></tr></table></figure><h3 id="文件压缩类命令"><a href="#文件压缩类命令" class="headerlink" title="文件压缩类命令"></a>文件压缩类命令</h3><h4 id="tar命令"><a href="#tar命令" class="headerlink" title="tar命令"></a>tar命令</h4><p>作用：备份文件<br>格式：tar [选项]  压缩文件名  路径</p><table><thead><tr><th>选项</th><th>描述</th></tr></thead><tbody><tr><td>-c</td><td>创建备份</td></tr><tr><td>-C  path</td><td>切换到指定目录</td></tr><tr><td>-f</td><td>指定备份文件</td></tr><tr><td>-t</td><td>测试备份文件</td></tr><tr><td>-v</td><td>显示指令执行过程</td></tr><tr><td>-x</td><td>从备份中还原文件</td></tr><tr><td>-z</td><td>通过gzip指令处理备份文件</td></tr></tbody></table><h4 id="gzip命令"><a href="#gzip命令" class="headerlink" title="gzip命令"></a>gzip命令</h4><p>作用：用于压缩文件<br>格式：gzip [选项] 文件…</p><table><thead><tr><th>选项</th><th>描述</th></tr></thead><tbody><tr><td>-d</td><td>解开压缩文件</td></tr><tr><td>-l</td><td>列出压缩文件的相关信息</td></tr><tr><td>-r</td><td>递归处理指定目录</td></tr><tr><td>-v</td><td>显示指令执行过程</td></tr><tr><td>-t</td><td>测试压缩文件是否有误</td></tr></tbody></table><h4 id="unzip命令"><a href="#unzip命令" class="headerlink" title="unzip命令"></a>unzip命令</h4><p>作用：用于解压缩zip文件<br>格式：unzip [选项] 路径</p><table><thead><tr><th>选项</th><th>描述</th></tr></thead><tbody><tr><td>-n</td><td>解压时不要覆盖原有文件</td></tr><tr><td>-l</td><td>列出压缩文件的相关信息</td></tr><tr><td>-d  dir</td><td>指定解压时存放的目录</td></tr><tr><td>-v</td><td>显示指令执行过程</td></tr><tr><td>-t</td><td>测试压缩文件是否有误</td></tr></tbody></table><h3 id="文本处理类命令"><a href="#文本处理类命令" class="headerlink" title="文本处理类命令"></a>文本处理类命令</h3><h4 id="awk命令"><a href="#awk命令" class="headerlink" title="awk命令"></a>awk命令</h4><p>作用：强大的文本数据处理工具</p><p>格式：awk [选项] ‘脚本命令’ 文件名</p><p>​    cat 文件名 | awk [选项] ‘脚本命令’</p><table><thead><tr><th>选项</th><th>描述</th></tr></thead><tbody><tr><td>-F  fs</td><td><strong>指定分隔符 fs</strong>，fs可以使字符串或正则表达式</td></tr><tr><td>-v</td><td>赋值一个用户定义变量</td></tr><tr><td>-f  scriptfile</td><td>从脚本文件中读取awk命令</td></tr></tbody></table><p>例子</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs shell"><span class="hljs-meta prompt_"># </span><span class="language-bash">将/etc/passwd文件中的每行以冒号为分隔符进行分割，然后输出每行的第1和第5个字段，也就是用户名和用户描述。其中awk命令使用了管道符号（|）将<span class="hljs-built_in">cat</span>命令的输出作为awk命令的输入。</span><br>cat /etc/passwd | awk -F: ‘&#123;print $1,$5&#125;’<br><span class="hljs-meta prompt_"># </span><span class="language-bash">直接将/etc/passwd文件作为awk命令的输入，使用了awk命令的另一种形式。同样是以冒号为分隔符进行分割，输出每行的第1和第5个字段。</span><br>awk -F: ‘&#123;print $1,$5&#125;’ /etc/passwd<br><span class="hljs-meta prompt_"># </span><span class="language-bash">在第一个例子的基础上增加了一个参数a，该参数的值为1。awk命令在处理每行时，输出的第2个字段是原来的第4个字段加上参数a的值。需要注意的是，在awk命令中使用变量时，需要在变量名前面加上前缀“-v”，否则awk命令会将变量名视为一个字符串。</span><br>cat /etc/passwd | awk -va=1 -F: ‘&#123;print $1,$4+a&#125;’<br></code></pre></td></tr></table></figure><h4 id="sed命令"><a href="#sed命令" class="headerlink" title="sed命令"></a>sed命令</h4><p>作用：自动编辑一个或多个文件、简化对文件的反复操作、编写转换程序等<br>格式：sed [选项] 处理格式路径</p><table><thead><tr><th>选项</th><th>描述</th></tr></thead><tbody><tr><td>a</td><td>在指定行号之后插入</td></tr><tr><td>c</td><td>整行替换</td></tr><tr><td>s</td><td>匹配替换第一个</td></tr><tr><td>i</td><td>在指定行号位置插入</td></tr><tr><td>d</td><td>删除</td></tr></tbody></table><p>例子</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><code class="hljs shell"><span class="hljs-meta prompt_"># </span><span class="language-bash">在第一行后插入一行 <span class="hljs-string">&quot;Hello Unix&quot;</span>：</span><br>sed &#x27;1a Hello Unix&#x27; test.txt<br><span class="hljs-meta prompt_"># </span><span class="language-bash">在第二行后插入一行 <span class="hljs-string">&quot;Hello Unix&quot;</span>：</span><br>sed &#x27;2a Hello Unix&#x27; test.txt<br><span class="hljs-meta prompt_"># </span><span class="language-bash">将第二行替换成 <span class="hljs-string">&quot;Hello Unix&quot;</span>：</span><br>sed &#x27;2c Hello Unix&#x27; test.txt<br><span class="hljs-meta prompt_"># </span><span class="language-bash">将所有的 <span class="hljs-string">&quot;l&quot;</span> 替换成 <span class="hljs-string">&quot;L&quot;</span>：</span><br>sed &#x27;s/l/L/&#x27; test.txt<br><span class="hljs-meta prompt_"># </span><span class="language-bash">将所有的 <span class="hljs-string">&quot;l&quot;</span> 替换成 <span class="hljs-string">&quot;L&quot;</span>，并且替换每一行中的所有匹配项：</span><br>sed &#x27;s/l/L/g&#x27; test.txt<br><span class="hljs-meta prompt_"># </span><span class="language-bash">在第一行前插入 <span class="hljs-string">&quot;Hello Unix&quot;</span>：</span><br>sed &#x27;1i Hello Unix&#x27; test.txt<br><span class="hljs-meta prompt_"># </span><span class="language-bash">在第二行前插入 <span class="hljs-string">&quot;Hello Unix&quot;</span>：</span><br>sed &#x27;2i Hello Unix&#x27; test.txt<br><span class="hljs-meta prompt_"># </span><span class="language-bash">删除第一行：</span><br>sed &#x27;1d&#x27; test.txt<br><span class="hljs-meta prompt_"># </span><span class="language-bash">删除所有行：</span><br>sed &#x27;d&#x27; test.txt<br><span class="hljs-meta prompt_"># </span><span class="language-bash">使用 -i 选项可以直接在原文件中进行修改</span><br><span class="hljs-meta prompt_"># </span><span class="language-bash">将 test.txt 文件的第二行替换为 Hello Unix：</span><br>sed -i &#x27;2c Hello Unix&#x27; test.txt<br><span class="hljs-meta prompt_"># </span><span class="language-bash">在 test.txt 文件的第二行之前插入一行 Hello Unix：</span><br>sed -i &#x27;2i Hello Unix&#x27; test.txt<br></code></pre></td></tr></table></figure><h4 id="cut命令"><a href="#cut命令" class="headerlink" title="cut命令"></a>cut命令</h4><p>作用：从指定文件中过滤或提取特定内容，并显示在当前屏幕上<br>格式：cut [选项] 路径</p><table><thead><tr><th>选项</th><th>描述</th></tr></thead><tbody><tr><td>-b</td><td>以字节为单位进行分割</td></tr><tr><td>-c</td><td>以字符为单位进行分割</td></tr><tr><td>-d</td><td>自定义分隔符，默认为制表符</td></tr><tr><td>-f</td><td>与-d一起使用，指定显示哪个区域</td></tr></tbody></table><p>例子</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs shell"><span class="hljs-meta prompt_"># </span><span class="language-bash">命令将以冒号为分隔符，提取 /etc/passwd 文件的第1、3和5列（即用户名、用户ID和用户描述），并将结果输出到标准输出。</span><br>cut -d: -f1,3,5 /etc/passwd <br><span class="hljs-meta prompt_"># </span><span class="language-bash">以字符为单位，提取 /etc/passwd 文件的第2到第5个字符，而 <span class="hljs-built_in">cut</span> -c2,5,7 /etc/passwd 则提取第2、5和7个字符。</span><br>cut -c2-5 /etc/passwd <br></code></pre></td></tr></table></figure><h4 id="tr命令"><a href="#tr命令" class="headerlink" title="tr命令"></a>tr命令</h4><p>作用：用于转换或删除文件中的字符<br>格式：tr [-d] 字符串1 字符串2</p><p>例子</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs shell"><span class="hljs-meta prompt_"># </span><span class="language-bash">将 /etc/passwd 文件中所有小写字母转换为大写字母，然后将结果输出到标准输出</span><br>cat /etc/passwd | tr ‘a-z’ ‘A-Z’<br><span class="hljs-meta prompt_"># </span><span class="language-bash">将 /etc/passwd 文件中所有小写字母转换为大写字母，然后将转换后的结果重定向到 <span class="hljs-built_in">tr</span> 命令的标准输入</span><br>tr ‘a-z’ ‘A-Z’ &lt; /etc/passwd<br><span class="hljs-meta prompt_"># </span><span class="language-bash">从 test.txt 文件中删除所有的 a-c 字符，然后将结果输出到标准输出</span><br>tr -d ‘a-c’ &lt; test.txt<br></code></pre></td></tr></table></figure><h3 id="查找类命令"><a href="#查找类命令" class="headerlink" title="查找类命令"></a>查找类命令</h3><h4 id="find命令"><a href="#find命令" class="headerlink" title="find命令"></a>find命令</h4><p>作用：查找文件或者目录<br>格式：find 路径 [选项] 表达式</p><table><thead><tr><th>选项</th><th>描述</th></tr></thead><tbody><tr><td>-amin  n</td><td>在过去n<strong>分钟</strong>内被<strong>读取</strong>过的文件</td></tr><tr><td>-atime  n</td><td>在过去n<strong>天</strong>内被<strong>读取</strong>过的文件</td></tr><tr><td>-cmin  n</td><td>在过去n<strong>分钟</strong>内被<strong>修改</strong>过的文件</td></tr><tr><td>-ctime  n</td><td>在过去n<strong>天</strong>内被<strong>修改</strong>过的文件</td></tr><tr><td>-type  c</td><td>文件<strong>类型</strong>是c的文件</td></tr><tr><td>-perm  p</td><td>文件<strong>权限</strong>为p的文件</td></tr><tr><td>-name  n</td><td><strong>文件名</strong>为n的文件</td></tr></tbody></table><p>例子</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs shell"><span class="hljs-meta prompt_"># </span><span class="language-bash">查找当前目录及其子目录下的所有扩展名为 .conf 的文件：</span><br>find . -name “*.conf”<br><span class="hljs-meta prompt_"># </span><span class="language-bash">查找根目录下所有权限为 765 的扩展名为 .txt 的文件：</span><br>find / -perm 765 -name “*.txt”<br><span class="hljs-meta prompt_"># </span><span class="language-bash">查找 /etc 目录下所有类型为文件的对象，并对每一个对象执行 <span class="hljs-built_in">ls</span> -l 命令：</span><br><span class="hljs-meta prompt_"># </span><span class="language-bash">-<span class="hljs-built_in">type</span> f 表示只查找类型为文件的对象，</span><br><span class="hljs-meta prompt_"># </span><span class="language-bash">-<span class="hljs-built_in">exec</span> 表示对每一个查找到的对象执行指定的命令，</span><br><span class="hljs-meta prompt_"># </span><span class="language-bash"><span class="hljs-built_in">ls</span> -l <span class="hljs-string">&#x27;&#123;&#125;&#x27;</span> \; 表示执行 <span class="hljs-built_in">ls</span> -l 命令，并将查找到的对象传递给 &#123;&#125; 占位符，\; 表示命令结束符号。</span><br>find /etc -type f -exec ls -l ‘&#123;&#125;’ \;<br></code></pre></td></tr></table></figure><h4 id="grep命令"><a href="#grep命令" class="headerlink" title="grep命令"></a>grep命令</h4><p>作用：查找文件里符合条件的字符串<br>格式：grep [选项] pattern文件…<br>           cat 文件… ｜ grep [选项] pattern\</p><table><thead><tr><th>选项</th><th>描述</th></tr></thead><tbody><tr><td>-i</td><td>忽略字符大小写的差别</td></tr><tr><td>-v</td><td>显示不包含匹配文本的所有行</td></tr><tr><td>-n</td><td>显示匹配行及行号</td></tr></tbody></table><p>例子</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs shell"><span class="hljs-meta prompt_"># </span><span class="language-bash">在 /etc/passwd 文件中查找包含 <span class="hljs-string">&quot;root&quot;</span> 的行，并输出到标准输出</span><br>grep ‘root’ /etc/passwd<br><span class="hljs-meta prompt_"># </span><span class="language-bash">将 /etc/passwd 文件的内容输出到标准输出，并使用管道将其作为输入传递给 grep 命令，查找包含 <span class="hljs-string">&quot;root&quot;</span> 的行并输出到标准输出</span><br>cat /etc/passwd | grep ’root’<br><span class="hljs-meta prompt_"># </span><span class="language-bash">将 /etc/passwd 文件的内容输出到标准输出，并使用管道将其作为输入传递给 grep 命令，查找不包含 <span class="hljs-string">&quot;root&quot;</span> 的行并输出到标准输出</span><br>cat /etc/passwd | grep -v ‘root’<br><span class="hljs-meta prompt_"># </span><span class="language-bash">将 /etc/passwd 文件的内容输出到标准输出，并使用管道将其作为输入传递给 grep 命令，查找包含 <span class="hljs-string">&quot;net&quot;</span> 或 <span class="hljs-string">&quot;Net&quot;</span> 或 <span class="hljs-string">&quot;NET&quot;</span> 的行，并输出到标准输出。</span> <br><span class="hljs-meta prompt_"># </span><span class="language-bash">-i 选项表示忽略大小写。</span><br>cat /etc/passwd | grep -i ‘net’<br></code></pre></td></tr></table></figure><h3 id="软连接和硬连接"><a href="#软连接和硬连接" class="headerlink" title="软连接和硬连接"></a>软连接和硬连接</h3><p><a href="https://www.zhihu.com/tardis/zm/art/619264530?source_id=1005">https://www.zhihu.com/tardis/zm/art/619264530?source_id=1005</a></p><h4 id="Linux文件存储"><a href="#Linux文件存储" class="headerlink" title="Linux文件存储"></a>Linux文件存储</h4><p>文件结构</p><ol><li>名字</li><li>索引节点，又称I节点，存放文件的状态信息的一个数据结构。包含文件的长度、创建及修改时间、权限、所属关系、磁盘位置等。</li><li>磁盘数据</li></ol><p><img src="/images/linux008.jpg"></p><p>硬链接：文件（副本）</p><p>软链接：文件、目录（1、长路径-&gt;短路径，方便；2、解决磁盘空间不足问题[跨文件系统链接]）</p><p>区别：</p><ol><li>文件类型不同：硬连接的连接文件和原文件在文件系统上没有区别，都是普通文件；软连接则是一个特殊的文件类型。</li><li>文件属性不同：硬链接和原文件的所有属性（如文件类型、权限、所有者等）都是相同的，而软链接则有自己的文件属性，它的文件类型是链接文件，而不是原文件的类型。</li><li>inode不同：硬链接和软链接都是链接，但它们链接不同inode节点，因为硬链接和原文件共享同一个inode节点，而软链接则指向了一个新的inode节点。</li><li>链接数不同：硬链接和原文件共享同一个inode节点，它们的链接数相同，而软链接的链接数始终为1。</li><li>跨文件系统：硬链接不能跨越不同的文件系统，软链接可以跨越不同的文件系统。</li><li>修改方式不同：修改硬链接和软链接的方式不同，修改硬链接等同于修改原文件，而修改软链接只是修改链接文件，不会修改目标文件。</li><li>适用场景不同：硬链接只能连接文件，不能连接目录；软链接可以连接文件和目录。</li><li>对链接进行操作（修改、删除）对原文件的影响：修改任何一个硬链接文件，都会改变文件内容，删除任何一个硬链接文件并不影响其他相同的inode号的文件，直到删除所有硬链接和源文件,才能删除改文件；删除、修改软链接不会影响原文件。</li><li>对原文件进行操作（修改、删除）对链接的影响：硬链接同上；原文件被删除那么软链接失效，修改源文件也不会改变链接文件的内容。</li></ol><p>联系：</p><ol><li>硬连接和软连接都是指向原文件的指针，链接文件的存在依赖于原文件，删除原文件会使链接文件失效。</li><li>硬连接和软连接都是为了方便文件的共享和管理。</li></ol><p>总的来说，硬链接更像是对原文件的副本，而软链接更像是对原文件的快捷方式。使用哪种方式取决于具体的需求和应用场景。</p><h3 id="常见正则表达式"><a href="#常见正则表达式" class="headerlink" title="常见正则表达式"></a>常见正则表达式</h3><ul><li>^  锚定行的开始 如：’^grep’匹配所有以grep开头的行 </li><li>$  锚定行的结束 如：’grep$’匹配所有以grep结尾的行</li><li>.  匹配一个非换行符的字符 如：’gr.p’匹配gr后接一个任意字符，然后是p </li><li>*  匹配零个或多个先前字符 如：’grep’匹配所有一个或多个空格后紧跟grep的行。.*一起用代表任意字符 </li><li>[]  匹配一个指定范围内的字符，如’[Gg]rep’匹配Grep和grep </li><li>[^]  匹配一个不在指定范围内的字符，如：’[^A-FH-Z]rep’匹配不包含A-F和H-Z的一个字母开头，紧跟rep的行 </li><li>\(..\)  标记匹配字符，如’\(love\)’，love被标记为1  </li><li>\&lt;  锚定单词的开始，如:’\&lt;grep’匹配包含以grep开头的单词的行 </li><li>\&gt;  锚定单词的结束，如’grep\&gt;’匹配包含以grep结尾的单词的行  </li><li>x\{m\}  重复字符x，m次，如：’0\{5\}’匹配包含5个o的行 </li><li>x\{m,\}  重复字符x,至少m次，如：’o\{5,\}’匹配至少有5个o的行  </li><li>x\{m,n\}  重复字符x，至少m次，不多于n次，如：’o\{5,10\}’匹配5–10个o的行 </li><li>\w  匹配文字和数字字符，也就是[A-Za-z0-9]，如：’G\w*p’匹配以G后跟零个或多个文字或数字字符，然后是p </li><li>\W  \w的反置形式，匹配一个或多个非单词字符，如点号句号等 </li><li>\b  单词锁定符，如: ‘\bgrep\b’只匹配grep</li></ul>]]></content>
    
    
    <categories>
      
      <category>Linux</category>
      
    </categories>
    
    
  </entry>
  
  
  
  <entry>
    <title>Docker运用实战</title>
    <link href="/2023/05/02/Docker%E8%BF%90%E7%94%A8%E5%AE%9E%E6%88%98/"/>
    <url>/2023/05/02/Docker%E8%BF%90%E7%94%A8%E5%AE%9E%E6%88%98/</url>
    
    <content type="html"><![CDATA[<h1 id="Docker-运用实战"><a href="#Docker-运用实战" class="headerlink" title="Docker 运用实战"></a>Docker 运用实战</h1><p>目前只实现了Java语言的部分。</p><p>TODO : Python、Go …</p><h2 id="目标"><a href="#目标" class="headerlink" title="目标"></a>目标</h2><p>利用 docker &#x2F; docker-comose 工具完成至少3种不同动态语言（ PHP &#x2F; Java &#x2F; Python &#x2F; Go &#x2F; Perl &#x2F; Nodejs …）运行环境的搭建，并实现从 mysql &#x2F; mongodb 等数据库中读取数据到 Web 页面中显示。</p><h2 id="步骤"><a href="#步骤" class="headerlink" title="步骤"></a>步骤</h2><h3 id="安装-docker"><a href="#安装-docker" class="headerlink" title="安装 docker"></a>安装 docker</h3><ol><li><p>如果此前安装过 docker，那么先卸载旧版本 docker 相关的内容</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><code class="hljs shell">yum remove docker \<br>                  docker-client \<br>                  docker-client-latest \<br>                  docker-common \<br>                  docker-latest \<br>                  docker-latest-logrotate \<br>                  docker-logrotate \<br>                  docker-selinux \<br>                  docker-engine-selinux \<br>                  docker-engine \<br>                  docker-ce<br></code></pre></td></tr></table></figure></li><li><p>安装命令</p><p>官方安装命令</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs shell">curl -fsSL https://get.docker.com | bash -s docker --mirror Aliyun<br></code></pre></td></tr></table></figure><blockquote><p>curl -fsSL <a href="https://get.docker.com：">https://get.docker.com：</a> 下载脚本文件</p><p>bash：运行该脚本文件</p><p> -s docker –mirror Aliyun：参数传入，使用阿里云源</p></blockquote><p>国内 daocloud 一键安装命令</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs shell">curl -sSL https://get.daocloud.io/docker | sh<br></code></pre></td></tr></table></figure></li><li><p>启动</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs shell">systemctl start docker<br></code></pre></td></tr></table></figure></li><li><p>加入开机启动</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs shell">systemctl enable docker<br></code></pre></td></tr></table></figure></li><li><p>安装成功</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs shell">docker -v<br></code></pre></td></tr></table></figure><p><img src="/images/docker001.jpg"></p></li><li><p>配置镜像加速源</p><p>输入指令：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs shell">vim /etc/docker/daemon.json<br></code></pre></td></tr></table></figure><p>切换至编辑模式，添加以下内容，并保存</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs shell">&#123;<br>   &quot;registry-mirrors&quot;: [<br>   &quot;https://mirror.ccs.tencentyun.com&quot;<br>  ]<br>&#125;<br></code></pre></td></tr></table></figure><p>这样拉取某些镜像就嘎嘎快了</p><p>别忘记重启下docker ps：如果此前有正在运行的容器，那么重启后需要重新运行容器</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs shell">systemctl restart docker<br></code></pre></td></tr></table></figure></li></ol><h3 id="安装-docker-compose"><a href="#安装-docker-compose" class="headerlink" title="安装 docker-compose"></a>安装 docker-compose</h3><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs shell"><span class="hljs-meta prompt_"># </span><span class="language-bash">安装</span><br>curl -L https://github.com/docker/compose/releases/download/1.23.1/docker-compose-`uname -s`-`uname -m` &gt; /usr/local/bin/docker-compose<br><span class="hljs-meta prompt_"># </span><span class="language-bash">修改权限</span><br>chmod +x /usr/local/bin/docker-compose<br></code></pre></td></tr></table></figure><p>安装成功</p><p><img src="/images/docker002.jpg"></p><h3 id="拉取-Mysql-镜像"><a href="#拉取-Mysql-镜像" class="headerlink" title="拉取 Mysql 镜像"></a>拉取 Mysql 镜像</h3><ol><li>拉取5.7版本的 mysql 镜像</li></ol><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs shell">docker pull mysql:5.7.25<br></code></pre></td></tr></table></figure><ol start="2"><li>查看 mysql 镜像</li></ol><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs shell">docker images mysql<br></code></pre></td></tr></table></figure><p><img src="/images/docker003.jpg"></p><ol start="3"><li>运行 mysql 容器</li></ol> <figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs shell">docker run --name=mysql -p 3306:3306 \<br>-e  MYSQL_ROOT_PASSWORD=123456 \<br>-v /mydata/mysql/log:/var/log/mysql \<br>-d  mysql:5.7.25<br></code></pre></td></tr></table></figure><ol start="4"><li><p>进入mysql容器查看版本号</p><p><img src="/images/docker004.jpg"></p></li><li><p>navicat 远程连接数据库并导入表结构以及数据</p></li></ol><h3 id="准备jar包以及jdk"><a href="#准备jar包以及jdk" class="headerlink" title="准备jar包以及jdk"></a>准备jar包以及jdk</h3><ol><li>在&#x2F;root下创建java-docker-compose-test文件夹</li><li>利用xftp将java-demo.jar、jdk8.tar.gz传输到此文件夹</li></ol><h3 id="编写Dockerfile文件"><a href="#编写Dockerfile文件" class="headerlink" title="编写Dockerfile文件"></a>编写Dockerfile文件</h3>   <figure class="highlight txt"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><code class="hljs txt"># 指定基础镜像<br>FROM ubuntu:16.04<br># 配置环境变量，JDK的安装目录<br>ENV JAVA_DIR=/usr/local<br><br># 拷贝jdk和java项目的包<br>COPY ./jdk8.tar.gz $JAVA_DIR/<br>COPY ./java-demo.jar /tmp/app.jar<br><br># 安装JDK<br>RUN cd $JAVA_DIR \<br> &amp;&amp; tar -xf ./jdk8.tar.gz \<br> &amp;&amp; mv ./jdk1.8.0_144 ./java8<br><br># 配置环境变量<br>ENV JAVA_HOME=$JAVA_DIR/java8<br>ENV PATH=$PATH:$JAVA_HOME/bin<br><br># 暴露端口<br>EXPOSE 8080<br># 入口，java项目的启动命令<br>ENTRYPOINT java -jar /tmp/app.jar<br></code></pre></td></tr></table></figure><ol><li>在java-docker-compose-test目录下运行如下指令</li></ol><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs shell">docker build -t java-docker-demo:1.0 .<br></code></pre></td></tr></table></figure><p>​ps ：注意末尾的点</p><ol start="2"><li>运行容器</li></ol><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs shell">docker run --name java-demo -p 8080:8080 --link mysql -d java-docker-demo:1.0<br></code></pre></td></tr></table></figure><h3 id="测试接口"><a href="#测试接口" class="headerlink" title="测试接口"></a>测试接口</h3><p>地址栏输入虚拟机ip:8080&#x2F;get&#x2F;201929210220，得到数据</p><p><img src="/images/docker005.jpg"></p><h2 id="完善-阶段一"><a href="#完善-阶段一" class="headerlink" title="完善-阶段一"></a>完善-阶段一</h2><p>到目前为止，我是创建了两个镜像，一个镜像只运行mysql5.7.25，另一个容器基于ubuntu基础镜像运行SpringBoot项目，项目是一个简单的SpringBoot项目，从数据库中读取数据，具体的效果体现为接口请求。</p><p>我在思考将这两个镜像合并成一个镜像，或许这种方案不是最佳解决方案，但重在一个学习过程。</p><h3 id="Dockerfile"><a href="#Dockerfile" class="headerlink" title="Dockerfile"></a>Dockerfile</h3><figure class="highlight dockerfile"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br></pre></td><td class="code"><pre><code class="hljs dockerfile"><span class="hljs-keyword">FROM</span> ubuntu:latest<br><br><span class="hljs-comment"># 配置环境变量，JDK的安装目录</span><br><span class="hljs-keyword">ENV</span> JAVA_DIR=/usr/local<br><br><span class="hljs-comment"># 拷贝jdk和java项目的包</span><br><span class="hljs-keyword">COPY</span><span class="language-bash"> ./jdk8.tar.gz <span class="hljs-variable">$JAVA_DIR</span>/</span><br><span class="hljs-keyword">COPY</span><span class="language-bash"> ./java-demo.jar /tmp/app.jar</span><br><br><span class="hljs-comment"># 安装JDK</span><br><span class="hljs-keyword">RUN</span><span class="language-bash"> <span class="hljs-built_in">cd</span> <span class="hljs-variable">$JAVA_DIR</span> \</span><br><span class="language-bash"> &amp;&amp; tar -xf ./jdk8.tar.gz \</span><br><span class="language-bash"> &amp;&amp; <span class="hljs-built_in">mv</span> ./jdk1.8.0_144 ./java8</span><br> <br><span class="hljs-comment"># 配置环境变量</span><br><span class="hljs-keyword">ENV</span> JAVA_HOME=$JAVA_DIR/java8<br><span class="hljs-keyword">ENV</span> PATH=$PATH:$JAVA_HOME/bin<br><br><span class="hljs-comment"># apt-get命令安装mysql-server</span><br><span class="hljs-keyword">RUN</span><span class="language-bash"> apt-get update &amp;&amp; apt-get install -y mysql-server</span><br><br><span class="hljs-comment"># 修改mysqld.cnf文件中的bind-address参数，允许远程连接</span><br><span class="hljs-keyword">RUN</span><span class="language-bash"> service mysql start &amp;&amp; \</span><br><span class="language-bash">    mysql -u root -e <span class="hljs-string">&quot;CREATE USER &#x27;docker&#x27;@&#x27;%&#x27; IDENTIFIED BY &#x27;docker&#x27;;&quot;</span> &amp;&amp; \</span><br><span class="language-bash">    mysql -u root -e <span class="hljs-string">&quot;GRANT ALL PRIVILEGES ON *.* TO &#x27;docker&#x27;@&#x27;%&#x27;;&quot;</span> &amp;&amp; \</span><br><span class="language-bash">    mysql -u root -e <span class="hljs-string">&quot;FLUSH PRIVILEGES;&quot;</span> &amp;&amp; \</span><br><span class="language-bash">    service mysql stop</span><br>    <br><span class="hljs-comment"># 修改mysqld.cnf文件中的bind-address参数，允许远程连接</span><br><span class="hljs-keyword">RUN</span><span class="language-bash"> sed -i <span class="hljs-string">&#x27;s/127.0.0.1/0.0.0.0/g&#x27;</span> /etc/mysql/mysql.conf.d/mysqld.cnf</span><br><br><span class="hljs-comment"># 挂载MySQL数据存储目录到数据卷</span><br><span class="hljs-keyword">VOLUME</span><span class="language-bash"> [<span class="hljs-string">&quot;/var/lib/mysql&quot;</span>]</span><br><br><span class="hljs-comment"># 暴露端口</span><br><span class="hljs-keyword">EXPOSE</span> <span class="hljs-number">3306</span><br><span class="hljs-comment"># 暴露端口</span><br><span class="hljs-keyword">EXPOSE</span> <span class="hljs-number">8080</span><br><br><span class="hljs-comment"># 后台允许mysql服务然后允许springboot项目</span><br><span class="hljs-keyword">ENTRYPOINT</span><span class="language-bash"> mysqld_safe &amp; java -jar /tmp/app.jar</span><br><br><span class="hljs-comment"># apt-get update &amp;&amp; apt-get install -y vim</span><br><br></code></pre></td></tr></table></figure><h3 id="镜像创建"><a href="#镜像创建" class="headerlink" title="镜像创建"></a>镜像创建</h3><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs shell">docker build -t demo:v5 .<br></code></pre></td></tr></table></figure><h3 id="镜像运行"><a href="#镜像运行" class="headerlink" title="镜像运行"></a>镜像运行</h3><p>暴露两个端口，暴露8080端口供接口请求，暴露3306端口接口允许navicat远程操作数据库便于调试。</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs shell">docker run -d --name demo -p 3306:3306 -p 8080:8080 demo:v5<br></code></pre></td></tr></table></figure><h3 id="测试"><a href="#测试" class="headerlink" title="测试"></a>测试</h3><p>进入容器内部或者在服务器上用curl命令测试接口</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs shell"><span class="hljs-meta prompt_"># </span><span class="language-bash">基于ubuntu的基础镜像，先安装curl</span><br>apt-get update -y &amp;&amp; apt-get install curl -y<br><span class="hljs-meta prompt_"># </span><span class="language-bash">测试接口</span><br><span class="hljs-meta prompt_"># </span><span class="language-bash">curl localhost:8080/student/get/201929210220</span><br></code></pre></td></tr></table></figure><p>效果图</p><p><img src="/images/docker010.jpg"></p><h2 id="完善-阶段二"><a href="#完善-阶段二" class="headerlink" title="完善-阶段二"></a>完善-阶段二</h2><p>TODO:</p><ol><li>编写自动下载jdk以及配置java环境的脚本。</li><li>下载mysql-server后，编写一个脚本创建指定数据库并自动导入现有的数据库表结构以及数据。</li></ol><h2 id="遇到的问题"><a href="#遇到的问题" class="headerlink" title="遇到的问题"></a>遇到的问题</h2><h3 id="测试接口1"><a href="#测试接口1" class="headerlink" title="测试接口1"></a>测试接口1</h3><p>很尴尬，一开始最终测试的步骤这里，我总是页面显示500的状态码，后台日志也是这样：</p><p><img src="/images/docker006.jpg"></p><p>我的情况是本地环境下运行项目能得到正确的结果，本地远程连接mysql容器也能成功。</p><p>一开始我理解为运行SpringBoot项目的容器与运行mysql的容器无法进行连接，我初步的解决方案是docker运行加上–link参数，但还是没有解决这个问题。</p><p>最终，在我的不懈努力下(就是做了很多无用功)，以及星球里某位老哥的提醒下，我发现我的iptables防火墙是开启的，我尝试把他直接关闭：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs shell">service iptables stop<br></code></pre></td></tr></table></figure><p>之后重启docker以及两个容器：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs shell">systemctl restart docker<br>docker start mysql<br>docker start java-demo<br></code></pre></td></tr></table></figure><p>然后，我怀着沉重的心情又测了一遍，最终解决了问题。</p><h3 id="测试接口2"><a href="#测试接口2" class="headerlink" title="测试接口2"></a>测试接口2</h3><p>编写第二种dockerfile的过程中遇到了很多困难，总是无法下载mysql服务，最终选择apt-get命令</p><p>服务器内以及容器内测试接口都得到了正确结果，但本机测试却总也得不到数据，具体报错信息如下：</p><p><img src="/images/docker011.jpg"></p><p>注意到状态码为502，我显示查看了容器运行日志</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs shell">docker logs -f demo<br></code></pre></td></tr></table></figure><p>并没有上面报错信息</p><p>这时候我就有点晕了，折腾了很久，在室友的提示下打开游览器网络页面检查</p><p><img src="/images/docker012.jpg"></p><p>这里就很奇怪了，<em>Remote Address: 127.0.0.1:7890</em></p><p>明明游览器发送的url是 <em><a href="http://43.138.188.66:8080/student/get/201929210220">http://43.138.188.66:8080/student/get/201929210220</a></em>，但最终怎么会变成这种意想不到的结果呢？</p><p>最终还是在室友的提示下，发现了问题所在，很蛋疼，是因为科学上网的原因…</p><p>重新测试</p><p><img src="/images/docker013.jpg"></p><p>Remote Address也很正常！</p><p>所以，科学上网需谨慎！</p><h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>问题得到解决，但我想了想直接关闭防火墙iptables只是权宜之计，正解应该是开放防火墙指定端口。</p><p>查看iptables并放开8080端口</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br></pre></td><td class="code"><pre><code class="hljs shell"><span class="hljs-meta prompt_"># </span><span class="language-bash">查看iptables</span><br>cat /etc/sysconfig/iptables<br><span class="hljs-meta prompt_"># </span><span class="language-bash">sample configuration <span class="hljs-keyword">for</span> iptables service</span><br><span class="hljs-meta prompt_"># </span><span class="language-bash">you can edit this manually or use system-config-firewall</span><br><span class="hljs-meta prompt_"># </span><span class="language-bash">please <span class="hljs-keyword">do</span> not ask us to add additional ports/services to this default configuration</span><br>*filter<br>:INPUT ACCEPT [0:0]<br>:FORWARD ACCEPT [0:0]<br>:OUTPUT ACCEPT [0:0]<br>-A INPUT -m state --state RELATED,ESTABLISHED -j ACCEPT<br>-A INPUT -p icmp -j ACCEPT<br>-A INPUT -i lo -j ACCEPT<br>-A INPUT -p tcp -m state --state NEW -m tcp --dport 8888 -j ACCEPT<br>-A INPUT -p tcp -m state --state NEW -m tcp --dport 80 -j ACCEPT<br>-A INPUT -p tcp -m state --state NEW -m tcp --dport 23000 -j ACCEPT<br>-A INPUT -p tcp -m state --state NEW -m tcp --dport 22122 -j ACCEPT<br>-A INPUT -p tcp -m state --state NEW -m tcp --dport 22 -j ACCEPT<br>-A INPUT -p tcp -m state --state NEW -m tcp --dport 8080 -j ACCEPT<br>-A INPUT -p tcp -m state --state NEW -m tcp --dport 3306 -j ACCEPT<br>-A INPUT -p tcp -m tcp --dport 23 -j ACCEPT<br>-A INPUT -p tcp -m tcp --dport 21 -j ACCEPT<br>-A INPUT -j REJECT --reject-with icmp-host-prohibited<br>-A FORWARD -j REJECT --reject-with icmp-host-prohibited<br>COMMIT<br><span class="hljs-meta prompt_"># </span><span class="language-bash">放开8080端口</span><br>iptables -A INPUT -p tcp -m state --state NEW -m tcp --dport 8080 -j ACCEPT<br></code></pre></td></tr></table></figure><p>其次，还需要考虑能否直接在SpringBoot项目运行的环境中搭建mysql环境，即编写更加完善的Dockerfile文件。（已完成）</p><p>服务器上运行的话，以腾讯云轻量服务器为例，放开指定端口即可！</p><p><img src="/images/docker014.jpg"></p><h1 id="相关指令"><a href="#相关指令" class="headerlink" title="相关指令"></a>相关指令</h1><h2 id="Docker基本指令"><a href="#Docker基本指令" class="headerlink" title="Docker基本指令"></a>Docker基本指令</h2><h3 id="概述"><a href="#概述" class="headerlink" title="概述"></a>概述</h3><ul><li>Docker环境信息   info、version</li><li>镜像仓库命令      login、logout、pull、push、search</li><li>镜像管理          build、images、import、load、rmi、save、tag、commit</li><li>容器生命周期管理  create、exec、kill、pause、restart、rm、run、start、stop、unpause</li><li>容器运维操作      attach、export、inspect、port、ps、rename、stats、top、wait、cp、diff、update</li><li>容器资源管理      volume、network</li><li>系统信息日志      events、history、logs</li></ul><p>docker –help       #查看docker命令<br>docker info         #docker 详细信息，镜像和容器<br>docker version      #查看docker版本<br>docker images  # 查看docker镜像；</p><h3 id="具体列解释含义"><a href="#具体列解释含义" class="headerlink" title="具体列解释含义"></a>具体列解释含义</h3><ul><li>REPOSITORY  镜像仓库源                </li><li>TAG         镜像的标签                 </li><li>IMAGE ID    镜像id            </li><li>CREATED     创建时间             </li><li>SIZE        大小</li></ul><p>同一个仓库源可以有多个 TAG ，表示这个仓库源的不同版本，我们使用 REPOSITORY:TAG 来定义不同的镜像。</p><h3 id="镜像命令"><a href="#镜像命令" class="headerlink" title="镜像命令"></a>镜像命令</h3><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br></pre></td><td class="code"><pre><code class="hljs shell"><span class="hljs-meta prompt_"># </span><span class="language-bash">列出本地所有的镜像</span><br>docker images -a            <br><span class="hljs-meta prompt_"># </span><span class="language-bash">只显示镜像ID</span><br>docker images -q            <br><span class="hljs-meta prompt_"># </span><span class="language-bash">显示镜像的摘要信息</span><br>docker images --digests <br><span class="hljs-meta prompt_"># </span><span class="language-bash">显示完整的镜像信息</span><br><span class="hljs-meta prompt_"># </span><span class="language-bash">镜像下载</span><br>docker images --no-trunc    <br><span class="hljs-meta prompt_"># </span><span class="language-bash">从Docker Hub上下载tomcat镜像，默认是最新版本。等价于：docker pull tomcat:latest</span><br>docker pull tomcat  <br><span class="hljs-meta prompt_"># </span><span class="language-bash">选择指定版本下载</span><br>docker pull tomcat:8  <br><span class="hljs-meta prompt_"># </span><span class="language-bash">如果需要确认是否有需要下载的版本，可以在docker hub上查找</span><br><span class="hljs-meta prompt_"># </span><span class="language-bash">删除镜像命令</span><br><span class="hljs-meta prompt_"># </span><span class="language-bash">单个镜像删除，相当于：docker rmi java:latest</span><br>docker rmi java<br><span class="hljs-meta prompt_"># </span><span class="language-bash">强制删除(删除正在运行的镜像，注：以后台方式运行的不能被强制删除)</span><br>docker rmi -f java<br><span class="hljs-meta prompt_"># </span><span class="language-bash">多个镜像删除，不同镜像间以空格间隔</span><br>docker rmi -f java tomcat nginx<br><span class="hljs-meta prompt_"># </span><span class="language-bash">删除本地全部镜像</span><br>docker rmi -f $(docker images -q)<br><br></code></pre></td></tr></table></figure><h3 id="容器命令"><a href="#容器命令" class="headerlink" title="容器命令"></a>容器命令</h3><h4 id="查看容器"><a href="#查看容器" class="headerlink" title="查看容器"></a>查看容器</h4><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs shell"><span class="hljs-meta prompt_"># </span><span class="language-bash">查看正在运行的容器</span><br>docker ps<br><span class="hljs-meta prompt_"># </span><span class="language-bash">查看所有容器包括停止的容器</span><br>docker ps -a<br><span class="hljs-meta prompt_"># </span><span class="language-bash">-q参数，只显示container <span class="hljs-built_in">id</span></span><br>docker ps -q<br><span class="hljs-meta prompt_"># </span><span class="language-bash">查看容器详细信息</span><br>docker inspect demo1<br></code></pre></td></tr></table></figure><h4 id="容器启动与停止"><a href="#容器启动与停止" class="headerlink" title="容器启动与停止"></a>容器启动与停止</h4><figure class="highlight txt"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><code class="hljs txt"># 新建并启动容器<br># 参数：-i 以交互模式运行容器<br>#    -t 为容器重新分配一个伪输入终端<br>#      --name  为容器指定一个名称<br>docker run -i -t --name mycentos 镜像名称/镜像ID<br># 后台启动容器，参数：-d  已守护方式启动容器<br>docker run -d mycentos<br># 启动止容器<br>docker start 容器id<br># 重启容器<br>docker restart 容器id<br># 关闭容器<br>docker kill 容器id<br>docker stop 容器id<br>-t 参数让Docker分配一个伪终端并绑定到容器的标准输入上<br>-i 参数则让容器的标准输入保持打开。<br>-c 参数用于给运行的容器分配cpu的shares值<br>-m 参数用于限制为容器的内存信息，以 B、K、M、G 为单位<br>-v 参数用于挂载一个volume，可以用多个-v参数同时挂载多个volume<br>-p 参数用于将容器的端口暴露给宿主机端口 格式：host_port:container_port 或者 host_ip:host_port:container_port<br>--name 容器名称<br>--net 容器使用的网络<br>-d 创建一个后台运行容器<br></code></pre></td></tr></table></figure><h4 id="容器进入与退出"><a href="#容器进入与退出" class="headerlink" title="容器进入与退出"></a>容器进入与退出</h4><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><code class="hljs shell"><span class="hljs-meta prompt_"># </span><span class="language-bash">使用run方式在创建时进入</span><br>docker run -it centos /bin/bash<br><span class="hljs-meta prompt_"># </span><span class="language-bash">关闭容器并退出</span><br>exit<br><span class="hljs-meta prompt_"># </span><span class="language-bash">仅退出容器，不关闭</span><br><span class="hljs-meta prompt_"># </span><span class="language-bash">快捷键：Ctrl + P + Q</span><br><span class="hljs-meta prompt_">#</span><span class="language-bash">快捷键：Ctrl + Shift + P + Q</span><br><span class="hljs-meta prompt_"># </span><span class="language-bash">容器进程</span><br><span class="hljs-meta prompt_"># </span><span class="language-bash">top支持 ps 命令参数，格式：docker top [OPTIONS] CONTAINER [ps OPTIONS]</span><br><span class="hljs-meta prompt_"># </span><span class="language-bash">列出redis容器中运行进程</span><br>docker top tomcat<br><span class="hljs-meta prompt_">#</span><span class="language-bash">查看所有运行容器的进程信息</span><br>for i in  `docker ps |grep Up|awk &#x27;&#123;print $1&#125;&#x27;`;do echo \ &amp;&amp;docker top $i; done<br></code></pre></td></tr></table></figure><p>进入当前正在运行的容器<br>方式一：<br>docker exec -it 容器id bashShell(&#x2F;bin&#x2F;bash)<br>方式二：<br>docker attach 容器id bashShell(&#x2F;bin&#x2F;bash)</p><p>exec ：进入容器后，开启一个新的终端，可以再里面操作；<br>attach ：进入容器正在执行的终端，不会启动新的终端进程；</p><p>容器内拷贝文件到主机<br>docker cp 容器id:容器内路径  目的主机路径</p><h4 id="容器日志"><a href="#容器日志" class="headerlink" title="容器日志"></a>容器日志</h4><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs shell"><span class="hljs-meta prompt_"># </span><span class="language-bash">查看redis容器日志，默认参数</span><br>docker logs tomcat<br><span class="hljs-meta prompt_"># </span><span class="language-bash">查看redis容器日志，参数：-f  跟踪日志输出；-t   显示时间戳；--<span class="hljs-built_in">tail</span>  仅列出最新N条容器日志；</span><br>docker logs -f -t --tail=20 redis<br><span class="hljs-meta prompt_"># </span><span class="language-bash">查看容器redis从2021年08月10日后的最新10条日志</span><br>docker logs --since=&quot;2021-08-10&quot; --tail=10 redis<br></code></pre></td></tr></table></figure><h2 id="Linux基本指令"><a href="#Linux基本指令" class="headerlink" title="Linux基本指令"></a>Linux基本指令</h2><h3 id="iptables基本操作"><a href="#iptables基本操作" class="headerlink" title="iptables基本操作"></a>iptables基本操作</h3><h4 id="systemctl基本命令"><a href="#systemctl基本命令" class="headerlink" title="systemctl基本命令"></a>systemctl基本命令</h4><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><code class="hljs shell"><span class="hljs-meta prompt_">#</span><span class="language-bash">查看防火墙的状态</span><br>systemctl status iptables.service<br><span class="hljs-meta prompt_">#</span><span class="language-bash">停止防火墙</span><br>systemctl stop iptables.service    <br><span class="hljs-meta prompt_">#</span><span class="language-bash">启动防火墙</span><br>systemctl start iptables.service<br><span class="hljs-meta prompt_">#</span><span class="language-bash">重启防火墙</span><br>systemctl restart iptables.service<br><span class="hljs-meta prompt_">#</span><span class="language-bash">开机启动防火墙</span><br>systemctl enable iptables.service<br></code></pre></td></tr></table></figure><h4 id="service基本命令"><a href="#service基本命令" class="headerlink" title="service基本命令"></a>service基本命令</h4><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs shell"><span class="hljs-meta prompt_">#</span><span class="language-bash">查看防火墙状态</span><br>service iptables status            <br><span class="hljs-meta prompt_">#</span><span class="language-bash">启动防火墙</span><br>service iptables start<br><span class="hljs-meta prompt_">#</span><span class="language-bash">停止防火墙</span><br>service iptables stop<br><span class="hljs-meta prompt_">#</span><span class="language-bash">重启防火墙</span><br>service iptables restart<br></code></pre></td></tr></table></figure><h3 id="开放-x2F-阻止端口"><a href="#开放-x2F-阻止端口" class="headerlink" title="开放&#x2F;阻止端口"></a>开放&#x2F;阻止端口</h3><h4 id="查询防火墙端口"><a href="#查询防火墙端口" class="headerlink" title="查询防火墙端口"></a>查询防火墙端口</h4><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs shell">iptables -L -n<br>iptables -L -n --line-number<br></code></pre></td></tr></table></figure><h4 id="允许所有服务器访问指定端口-比如8080"><a href="#允许所有服务器访问指定端口-比如8080" class="headerlink" title="允许所有服务器访问指定端口 比如8080"></a>允许所有服务器访问指定端口 比如8080</h4><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs shell">iptables -A INPUT -p tcp -m state --state NEW -m tcp --dport 8080 -j ACCEPT<br></code></pre></td></tr></table></figure><p>参数说明</p><table><thead><tr><th>参数</th><th>说明</th></tr></thead><tbody><tr><td><code>-A</code></td><td>添加一条INPUT的规则</td></tr><tr><td><code>-p</code></td><td>指定是什么协议（TCP&#x2F;UDP）</td></tr><tr><td><code>--dport</code></td><td>就是目标端口，当数据从外部进入服务器为目标端口</td></tr><tr><td><code>--sport</code></td><td>数据从服务器出去，则为数据原端口</td></tr><tr><td><code>-j</code></td><td>就是指定是<code>ACCEPT</code>接受或阻止<code>DROP</code></td></tr></tbody></table><h2 id="问题"><a href="#问题" class="headerlink" title="问题"></a>问题</h2><h3 id="1-bash-ping-command-not-found-以及同类型的问题"><a href="#1-bash-ping-command-not-found-以及同类型的问题" class="headerlink" title="1.bash: ping: command not found 以及同类型的问题"></a>1.bash: ping: command not found 以及同类型的问题</h3><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs shell">apt-get update<br>apt-get install iputils-ping<br></code></pre></td></tr></table></figure><p>TODO</p><h1 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h1><p><a href="https://cloud.tencent.com/document/product/1207/45596">https://cloud.tencent.com/document/product/1207/45596</a></p><p><a href="https://blog.csdn.net/JustinQin/article/details/120936620">https://blog.csdn.net/JustinQin/article/details/120936620</a></p><p><a href="https://www.jianshu.com/p/30e13e06341a">https://www.jianshu.com/p/30e13e06341a</a></p>]]></content>
    
    
    <categories>
      
      <category>Linux</category>
      
      <category>Docker</category>
      
    </categories>
    
    
    <tags>
      
      <tag>原创</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Java并发编程之美</title>
    <link href="/2023/05/01/Java%E5%B9%B6%E5%8F%91%E7%BC%96%E7%A8%8B%E4%B9%8B%E7%BE%8E/"/>
    <url>/2023/05/01/Java%E5%B9%B6%E5%8F%91%E7%BC%96%E7%A8%8B%E4%B9%8B%E7%BE%8E/</url>
    
    <content type="html"><![CDATA[<h2 id="第二章"><a href="#第二章" class="headerlink" title="第二章"></a>第二章</h2><h3 id="Java内存模型"><a href="#Java内存模型" class="headerlink" title="Java内存模型"></a>Java内存模型</h3><ul><li>volatile只能保证可见性和有序性，但不能保证原子性。</li><li>volatile只能保证对单次读&#x2F;写的原子性，但不能保证对复合操作（如i++）的原子性。</li><li>volatile不会对变量加锁，因此多个线程同时访问一个变量时，可能会出现数据不一致的情况。</li></ul><p><a href="https://zhuanlan.zhihu.com/p/445741366">Volatile的实现原理(看这篇就够了) - 知乎</a><a href="https://zhuanlan.zhihu.com/p/445741366">1</a></p><h3 id="伪共享"><a href="#伪共享" class="headerlink" title="伪共享"></a>伪共享</h3><p>伪共享是多线程操作位于同一缓存行的不同变量时，由于缓存失效而引发性能下降的问题。缓存行是CPU缓存的最小单位，一般是64个字节，当CPU读取数据时，会把数据所在的缓存行一起读取。如果多个线程同时修改同一个缓存行中的不同变量，就会导致缓存行失效，需要重新从内存中读取，这样就降低了性能。<br>为了避免伪共享，有以下几种方法：</p><ul><li>在变量之间填充一些无用的变量，使得每个变量占据一个缓存行</li><li>使用消除了伪共享结构的类，如java.util.concurrent.atomic.LongAdder</li><li>使用@sun.misc.Contended注解（java8），让编译器自动填充空间</li></ul><h2 id="第四章"><a href="#第四章" class="headerlink" title="第四章"></a>第四章</h2><h3 id="AtomicLong与LongAdder"><a href="#AtomicLong与LongAdder" class="headerlink" title="AtomicLong与LongAdder"></a>AtomicLong与LongAdder</h3><p>前者是通过CAS提供非阻塞的原子性操作，但在高并发下大量线程会同时去竞争一个原子变量，由于同时只有一个线程会成功，这就造成大量线程竞争失败，会通过无限循环来不断进行自旋尝试CAS的操作，而这会白白浪费CPU资源，而LongAdder则是利用多个线程去竞争多个资源的思路来设计的。<br>LongAdder是Java 8新增的一个原子类，可以用于高并发下的计数器操作。相比于AtomicLong，LongAdder的性能更好，因为它将单一的变量内部拆分成多个变量，每个线程操作一个变量，最终将所有变量的值求和得到最终结果，从而减少了线程之间的竞争。<br>具体来说，LongAdder内部维护了一个Base变量和一个Cell数组，Base变量的初始值为0，Cell数组的初始长度为2。每个Cell都是一个原子的Long变量，表示分段计数器的值。当多个线程同时对LongAdder进行累加操作时，LongAdder会将累加任务分散到Cell数组中的不同Cell中进行处理1。每个线程对应一个Cell，只有在Cell的竞争激烈时才会扩容。<br>当多个线程获取LongAdder的值时，LongAdder会将Base和Cell数组中所有Cell的值相加，得到最终的结果。<br>总体来说，LongAdder通过将单一变量拆分成多个变量以及通过分段的方式减少了多线程的竞争，从而提高了高并发下的性能表现。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">import</span> java.util.concurrent.ExecutorService;<br><span class="hljs-keyword">import</span> java.util.concurrent.Executors;<br><span class="hljs-keyword">import</span> java.util.concurrent.TimeUnit;<br><span class="hljs-keyword">import</span> java.util.concurrent.atomic.LongAdder;<br><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">LongAdderExample</span> &#123;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">main</span><span class="hljs-params">(String[] args)</span> <span class="hljs-keyword">throws</span> InterruptedException &#123;<br>        <span class="hljs-type">LongAdder</span> <span class="hljs-variable">counter</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">LongAdder</span>();<br><br>        <span class="hljs-type">ExecutorService</span> <span class="hljs-variable">executorService</span> <span class="hljs-operator">=</span> Executors.newFixedThreadPool(<span class="hljs-number">10</span>);<br><br>        <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> <span class="hljs-variable">i</span> <span class="hljs-operator">=</span> <span class="hljs-number">0</span>; i &lt; <span class="hljs-number">100</span>; i++) &#123;<br>            executorService.submit(() -&gt; &#123;<br>                <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> <span class="hljs-variable">j</span> <span class="hljs-operator">=</span> <span class="hljs-number">0</span>; j &lt; <span class="hljs-number">10000</span>; j++) &#123;<br>                    counter.increment();<br>                &#125;<br>            &#125;);<br>        &#125;<br><br>        executorService.shutdown();<br>        executorService.awaitTermination(<span class="hljs-number">1</span>, TimeUnit.MINUTES);<br><br>        System.out.println(<span class="hljs-string">&quot;Counter: &quot;</span> + counter.sum());<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>在这个例子中，我们创建了一个LongAdder对象，并使用10个线程对计数器进行累加操作。每个线程执行了10000次累加操作。通过调用LongAdder对象的**increment()<strong>方法，我们可以避免在多线程情况下的竞争，从而提高程序的性能。<br>最后，我们通过调用LongAdder对象的</strong>sum()**方法，获取计数器的总和。在这个例子中，计数器的预期总和为100 * 10000 &#x3D; 1000000。</p><p>1 当多个线程同时对同一个LongAdder对象进行累加操作时，LongAdder内部实际上维护了一个Cell数组，每个Cell都包含一个long类型的值。这些Cell可以看作是多个独立的计数器，当线程对LongAdder对象进行累加时，LongAdder会根据线程的hashcode值将累加任务分散到不同的Cell中进行处理，以此实现了并发操作的效果。<br>例如，假设有三个线程Thread1、Thread2、Thread3同时对一个LongAdder对象进行累加操作，这个LongAdder内部维护了3个Cell，那么累加操作会被分散到这3个Cell中进行，例如Thread1的累加操作会被分散到Cell1中，Thread2的累加操作会被分散到Cell2中，Thread3的累加操作会被分散到Cell3中。最终，所有Cell中的值会被累加到一起，得到LongAdder对象最终的累加结果。这种方式可以避免多个线程对同一个计数器进行争用，从而提高了并发操作的性能。<br>例如，假设有10个线程同时对LongAdder进行递增，其中前5个线程递增的值为1，后5个线程递增的值为2。那么Cell数组中将会有5个Cell，前5个Cell的值为1，后5个Cell的值为2。最终，LongAdder将所有Cell的值相加，得到的结果为15。这样就保证了在高并发情况下LongAdder的准确性和可靠性。</p><h2 id="第五章"><a href="#第五章" class="headerlink" title="第五章"></a>第五章</h2><h3 id="CopyOnWriteArrayList"><a href="#CopyOnWriteArrayList" class="headerlink" title="CopyOnWriteArrayList"></a>CopyOnWriteArrayList</h3><p>CopyOnWriteArrayList 是 Java 并发集合框架中的一种，它是一个线程安全的 List，它通过复制底层数组的方式来实现并发更新，因此读操作不需要加锁，写操作则需要加锁。但是，如果使用 CopyOnWriteArrayList 的 get(int index) 方法获取指定位置的元素时，可能会发生并发问题。<br>具体来说，假设线程 A 和线程 B 同时调用 CopyOnWriteArrayList 的 get(int index) 方法获取同一个索引位置的元素，那么在 CopyOnWriteArrayList 内部会进行一次数组的复制操作，这会导致线程 A 和线程 B 都获取到不同的数组引用，因此线程 A 可能获取到索引位置处的旧元素，而线程 B 可能获取到索引位置处的新元素。<br>例如，假设 CopyOnWriteArrayList 内部的数组为 [1, 2, 3, 4]，线程 A 和线程 B 同时调用 get(2) 方法，那么 CopyOnWriteArrayList 内部会创建一个新数组 [1, 2, 3, 4]，并返回数组下标为 2 的元素。由于新数组是在获取元素时才创建的，因此线程 A 和线程 B 可能都获取到不同的数组引用，例如线程 A 获取到的是旧数组 [1, 2, 3, 4]，而线程 B 获取到的是新数组 [1, 2, 5, 4]。因此，线程 A 可能获取到索引位置 2 上的旧元素 3，而线程 B 可能获取到索引位置 2 上的新元素 5。<br>要避免这种并发问题，可以在获取元素时加锁，或者使用类似 AtomicIntegerArray 的线程安全数组。另外，由于 CopyOnWriteArrayList 适用于读多写少的场景，因此在需要频繁修改的场景下，不建议使用 CopyOnWriteArrayList。</p><h4 id="弱一致性迭代器"><a href="#弱一致性迭代器" class="headerlink" title="弱一致性迭代器"></a>弱一致性迭代器</h4><p>迭代器遍历的数组是一个快照<br>CopyOnWriteArrayList是一种线程安全的List集合，它的迭代器也是一种弱一致性迭代器。弱一致性迭代器的特点是在迭代过程中，如果有其他线程对集合中的元素进行了修改，那么当前迭代器的视图不一定能看到这些修改。<br>具体来说，当CopyOnWriteArrayList的迭代器被创建时，它会获取一份集合的快照，这个快照是迭代器可见的视图，迭代器在迭代时只会遍历这份快照中的元素。如果其他线程在迭代过程中修改了集合中的元素，这些修改不会影响到迭代器的遍历过程，因为迭代器的视图已经被固定了。<br>下面是一个简单的示例，演示了CopyOnWriteArrayList的弱一致性迭代器：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">import</span> java.util.Iterator;<br><span class="hljs-keyword">import</span> java.util.concurrent.CopyOnWriteArrayList;<br><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">CopyOnWriteArrayListExample</span> &#123;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">main</span><span class="hljs-params">(String[] args)</span> &#123;<br>        CopyOnWriteArrayList&lt;Integer&gt; list = <span class="hljs-keyword">new</span> <span class="hljs-title class_">CopyOnWriteArrayList</span>&lt;&gt;(<span class="hljs-keyword">new</span> <span class="hljs-title class_">Integer</span>[]&#123;<span class="hljs-number">1</span>, <span class="hljs-number">2</span>, <span class="hljs-number">3</span>, <span class="hljs-number">4</span>, <span class="hljs-number">5</span>&#125;);<br><br>        <span class="hljs-comment">// 创建迭代器</span><br>        Iterator&lt;Integer&gt; iterator = list.iterator();<br><br>        <span class="hljs-comment">// 遍历集合中的元素</span><br>        <span class="hljs-keyword">while</span> (iterator.hasNext()) &#123;<br>            <span class="hljs-type">Integer</span> <span class="hljs-variable">value</span> <span class="hljs-operator">=</span> iterator.next();<br>            System.out.print(value + <span class="hljs-string">&quot; &quot;</span>);<br><br>            <span class="hljs-comment">// 在迭代过程中添加一个元素</span><br>            <span class="hljs-keyword">if</span> (value == <span class="hljs-number">2</span>) &#123;<br>                list.add(<span class="hljs-number">6</span>);<br>            &#125;<br>        &#125;<br>        System.out.println();<br><br>        <span class="hljs-comment">// 输出集合中的元素</span><br>        System.out.println(list);<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>上述示例中，我们首先创建了一个CopyOnWriteArrayList对象，并在其中添加了5个元素。然后我们通过list.iterator()方法创建了一个迭代器，遍历了集合中的元素。在遍历过程中，当遍历到元素2时，我们向集合中添加了一个新的元素6。最后我们输出了集合中的元素。<br>由于CopyOnWriteArrayList的弱一致性迭代器只能看到在创建迭代器时集合中的元素，所以迭代器只会遍历到集合中的前5个元素。当我们向集合中添加了元素6时，迭代器并没有看到这个修改，因此输出的集合中还是只有5个元素。<br>总之，CopyOnWriteArrayList的弱一致性迭代器虽然不保证遍历到集合的最新状态，但它是线程安全的，不会抛出ConcurrentModificationException异常。如果在遍历过程中需要修改集合中的元素，建议使用ListIterator，它支持修改操作，但不是线程安全的。</p><h2 id="第六章"><a href="#第六章" class="headerlink" title="第六章"></a>第六章</h2><h3 id="LockSupport"><a href="#LockSupport" class="headerlink" title="LockSupport"></a>LockSupport</h3><p>LockSupport 是 Java 提供的一个线程同步工具，用于实现类似于 wait&#x2F;notify 的等待&#x2F;通知机制。它是一种比 synchronized 更加灵活和高效的线程同步方式。<br>LockSupport 的主要作用是阻塞和唤醒线程，其底层是使用了操作系统提供的底层函数（如 Linux 的 futex）来实现线程阻塞和唤醒。<br>LockSupport 类提供了 park() 和 unpark() 两个方法。park() 方法用于阻塞当前线程，unpark() 方法用于唤醒指定线程。<br>park() 方法可以响应中断和超时，并且在被唤醒时不会出现“虚假唤醒”的情况（也就是说，即使没有调用 unpark() 方法，线程也不会在 park() 方法中无限期地阻塞）。<br>LockSupport 的使用一般是配合其它的同步工具一起使用，比如 ReentrantLock、Semaphore 等。<br>以下是一个使用 LockSupport 实现线程阻塞和唤醒的示例代码：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">LockSupportDemo</span> &#123;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">main</span><span class="hljs-params">(String[] args)</span> &#123;<br>        <span class="hljs-type">Thread</span> <span class="hljs-variable">t1</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Thread</span>(() -&gt; &#123;<br>            System.out.println(Thread.currentThread().getName() + <span class="hljs-string">&quot; start...&quot;</span>);<br>            LockSupport.park(); <span class="hljs-comment">// 阻塞线程</span><br>            System.out.println(Thread.currentThread().getName() + <span class="hljs-string">&quot; end...&quot;</span>);<br>        &#125;);<br>        t1.start();<br><br>        <span class="hljs-keyword">try</span> &#123;<br>            Thread.sleep(<span class="hljs-number">1000</span>); <span class="hljs-comment">// 主线程睡眠1秒钟</span><br>        &#125; <span class="hljs-keyword">catch</span> (InterruptedException e) &#123;<br>            e.printStackTrace();<br>        &#125;<br><br>        System.out.println(Thread.currentThread().getName() + <span class="hljs-string">&quot; unpark t1...&quot;</span>);<br>        LockSupport.unpark(t1); <span class="hljs-comment">// 唤醒线程</span><br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>运行结果如下：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs bash">Thread-0 start...<br>main unpark t1...<br>Thread-0 end...<br></code></pre></td></tr></table></figure><p>可以看到，主线程在睡眠1秒钟后调用 unpark() 方法唤醒了 t1 线程，从而使得 t1 线程能够顺利结束。</p><h3 id="AQS"><a href="#AQS" class="headerlink" title="AQS"></a>AQS</h3><h4 id="锁的底层支持"><a href="#锁的底层支持" class="headerlink" title="锁的底层支持"></a>锁的底层支持</h4><p>AQS底层基于一个双向队列（FIFO）和一个CAS原子变量，用于实现阻塞和唤醒线程的操作。当一个线程请求锁时，如果锁已经被占用，则当前线程会被阻塞，并被放入等待队列中。当持有锁的线程释放锁时，AQS会唤醒等待队列中的第一个线程，将其从等待队列中取出，并尝试将其设置为锁的持有者。<br>AQS中的状态变量表示锁的状态，通过CAS原子操作来改变状态。当一个线程成功获取到锁时，会将状态变量设置为占用状态，并将自己设置为锁的持有者。当释放锁时，会将状态变量设置为未占用状态，并唤醒等待队列中的第一个线程。</p><h4 id="ConditionObject"><a href="#ConditionObject" class="headerlink" title="ConditionObject"></a>ConditionObject</h4><p>ConditionObject是AQS的内部类，是用来支持锁的条件等待和通知的。ConditionObject的主要作用是提供了一个类似Object.wait()和Object.notify()的机制，可以让线程在等待某个条件成立时挂起，并在条件满足时被唤醒。<br>ConditionObject的实现是基于一个FIFO双向链表的，每个链表节点对应一个等待线程。当一个线程调用了Condition.await()方法后，它会被放到这个链表的末尾，然后等待被唤醒。当有其他线程调用了Condition.signal()或Condition.signalAll()方法时，会从这个链表的头部开始唤醒线程，直到链表为空或者条件被满足。<br>ConditionObject内部维护了一个整数类型的状态值state，当调用await()方法时，线程会释放锁并进入阻塞状态，同时会将这个状态值保存在一个等待队列中。当其他线程调用signal()或signalAll()方法时，会唤醒等待队列中的一个或多个线程，并将它们从等待队列中移除。唤醒的线程会重新尝试获取锁，并从await()方法中返回，继续执行后面的代码。<br>总的来说，ConditionObject为锁的使用者提供了一种灵活的等待和通知机制，可以更好地满足一些高级应用场景的需求。</p><h4 id="条件变量支持"><a href="#条件变量支持" class="headerlink" title="条件变量支持"></a>条件变量支持</h4><p><code>一个锁对应一个AQS阻塞队列，对应多个条件变量，每个条件变量有自己的一个条件队列。</code></p><p>AQS（AbstractQueuedSynchronizer）提供了对条件变量的支持，条件变量是用来对线程进行等待和唤醒的机制。AQS中通过内部类<strong>ConditionObject</strong>来实现条件变量的支持，每个条件变量对象都与一个独立的等待队列（WaitQueue）相关联，等待队列中存储了所有正在等待条件变量的线程节点（Node）。<br><strong>ConditionObject</strong>类中定义了**await()<strong>、</strong>signal()<strong>和</strong>signalAll()<strong>等方法，用来实现线程等待和唤醒操作。当一个线程调用</strong>await()<strong>方法时，它会被加入到条件变量的等待队列中，然后被阻塞，直到被其他线程调用</strong>signal()<strong>或</strong>signalAll()**方法唤醒。<br>在AQS中，一个锁对象可以有多个条件变量对象。当一个线程持有一个锁对象时，它可以创建多个条件变量对象，并在条件变量对象上进行等待和唤醒操作。每个条件变量对象都是独立的，它们之间没有任何关联。<br>下面是一个简单的示例代码，演示了如何使用AQS的条件变量实现线程间的等待和唤醒：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">import</span> java.util.concurrent.locks.Condition;<br><span class="hljs-keyword">import</span> java.util.concurrent.locks.ReentrantLock;<br><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">ConditionExample</span> &#123;<br>    <span class="hljs-keyword">private</span> <span class="hljs-type">ReentrantLock</span> <span class="hljs-variable">lock</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">ReentrantLock</span>();<br>    <span class="hljs-keyword">private</span> <span class="hljs-type">Condition</span> <span class="hljs-variable">condition</span> <span class="hljs-operator">=</span> lock.newCondition();<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">doWait</span><span class="hljs-params">()</span> <span class="hljs-keyword">throws</span> InterruptedException &#123;<br>        lock.lock();<br>        <span class="hljs-keyword">try</span> &#123;<br>            System.out.println(Thread.currentThread().getName() + <span class="hljs-string">&quot; is waiting...&quot;</span>);<br>            condition.await();<br>            System.out.println(Thread.currentThread().getName() + <span class="hljs-string">&quot; is notified.&quot;</span>);<br>        &#125; <span class="hljs-keyword">finally</span> &#123;<br>            lock.unlock();<br>        &#125;<br>    &#125;<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">doNotify</span><span class="hljs-params">()</span> <span class="hljs-keyword">throws</span> InterruptedException &#123;<br>        lock.lock();<br>        <span class="hljs-keyword">try</span> &#123;<br>            System.out.println(Thread.currentThread().getName() + <span class="hljs-string">&quot; is notifying...&quot;</span>);<br>            condition.signal();<br>            System.out.println(Thread.currentThread().getName() + <span class="hljs-string">&quot; has notified.&quot;</span>);<br>        &#125; <span class="hljs-keyword">finally</span> &#123;<br>            lock.unlock();<br>        &#125;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>在上面的代码中，<strong>ConditionExample</strong>类使用了一个<strong>ReentrantLock</strong>对象来实现锁的功能，同时使用了一个<strong>Condition</strong>对象来实现条件变量的支持。<strong>doWait()<strong>方法通过调用</strong>condition.await()<strong>将当前线程加入到等待队列中并阻塞，</strong>doNotify()<strong>方法则通过调用</strong>condition.signal()<strong>唤醒等待队列中的一个线程。<br>使用AQS的条件变量可以方便地实现线程间的等待和唤醒操作，而不必依赖于Java中的</strong>synchronized</strong>关键字和**wait()<strong>、</strong>notify()**等方法。同时，AQS的条件变量还提供了更高级的等待机制，比如可以等待一定时间后自动返回，或者可以被中断等等。</p><h3 id="ReentrantLock"><a href="#ReentrantLock" class="headerlink" title="ReentrantLock"></a>ReentrantLock</h3><h4 id="类图结构"><a href="#类图结构" class="headerlink" title="类图结构"></a>类图结构</h4><p>ReentrantLock是可重入的独占锁，默认非公平锁，AQS的state状态值表示线程获取该锁的可重入次数。</p><h4 id="获取锁与释放锁"><a href="#获取锁与释放锁" class="headerlink" title="获取锁与释放锁"></a>获取锁与释放锁</h4><ul><li>获取锁：当线程通过lock()方法获取ReentrantLock的锁时，首先会判断当前线程是否已经持有该锁，如果是，则直接增加该线程持有的锁计数器即可，否则会尝试使用CAS操作去获取锁。如果获取成功，则将当前线程与该锁建立关联，并将该线程持有的锁计数器置为1，同时返回。</li><li>释放锁：当线程通过unlock()方法释放ReentrantLock的锁时，首先会判断当前线程是否持有该锁，如果不是，则抛出IllegalMonitorStateException异常，否则将该线程持有的锁计数器减1，如果减1后计数器的值为0，则该线程释放该锁。</li></ul><h4 id="公平锁与非公平锁"><a href="#公平锁与非公平锁" class="headerlink" title="公平锁与非公平锁"></a>公平锁与非公平锁</h4><p>在公平锁模式下，线程尝试获取锁的顺序按照它们发出请求的顺序进行，也就是说，先到先得。在 <strong>ReentrantLock</strong> 中，公平锁模式是通过 <strong>FairSync</strong> 实现的。<br>在非公平锁模式下，线程在尝试获取锁时不考虑自己以前的等待时间，而是直接尝试获取锁。这种模式可能会比公平锁模式更快，因为它允许线程插队并在竞争到锁时获得它。在 <strong>ReentrantLock</strong> 中，非公平锁模式是通过 <strong>NonfairSync</strong> 实现的。<br>公平锁和非公平锁的代码区别在于获取锁的方法不同：</p><ul><li>公平锁：tryAcquire(int acquires) 方法会调用父类的tryAcquire方法，而父类的tryAcquire方法会判断同步队列是否有前驱节点，如果有则表示有线程正在等待获取锁，此时当前线程就会进入同步队列等待前驱节点释放锁。</li><li>非公平锁：tryAcquire(int acquires) 方法会直接调用父类的nonfairTryAcquire方法，尝试通过CAS更新state状态值，如果更新成功则表示获取到了锁，否则进入同步队列等待唤醒。</li></ul><h3 id="ReentrantReadWriteLock"><a href="#ReentrantReadWriteLock" class="headerlink" title="ReentrantReadWriteLock"></a>ReentrantReadWriteLock</h3><p>采用读写分离的策略，允许多个线程可以同时获取读锁，主要是为了满足写少读多的场景。</p><h4 id="类图"><a href="#类图" class="headerlink" title="类图"></a>类图</h4><p>读写锁内部维护了一个ReadLock和一个WriteLock,依赖Sync实现具体功能。state高16位表示读状态，低16位表示获取写锁的可重入次数。</p><h4 id="写锁的获取与释放"><a href="#写锁的获取与释放" class="headerlink" title="写锁的获取与释放"></a>写锁的获取与释放</h4><p>独占锁，可重入。<br>如果当前没有线程获取到读锁和写锁，那么当前线程可以直接获取到写锁然后返回；如果当前已经有线程获取到读锁和写锁，那么请求写锁的线程会被挂起。</p><h4 id="读锁的获取与释放"><a href="#读锁的获取与释放" class="headerlink" title="读锁的获取与释放"></a>读锁的获取与释放</h4><p>如果没有线程持有写锁，则当前线程可以获取写锁并将AQS的状态值state高16位增加1；如果有线程持有写锁，持有写锁的不是当前线程，那么当前线程会被阻塞挂起，持有写锁的是当前线程那么也可以获取读锁。释放锁，循环判断AQS的状态值，如果为0，则说明没有读线程占用读锁，释放一个由于获取写锁被阻塞的线程；如果不为0，还有其他线程持有读锁。</p><h3 id="StampedLock"><a href="#StampedLock" class="headerlink" title="StampedLock"></a>StampedLock</h3><p>当调用获取锁的系列函数时，会返回一个long型的变量，这个戳记代表了锁的状态</p><h4 id="写锁writeLock"><a href="#写锁writeLock" class="headerlink" title="写锁writeLock"></a>写锁writeLock</h4><p>独占锁，不同于ReentrantReadWriteLock的是，这个锁是不可重入的。</p><h4 id="悲观读readLock"><a href="#悲观读readLock" class="headerlink" title="悲观读readLock"></a>悲观读readLock</h4><p>共享锁，不同于ReentrantReadWriteLock的是，这个锁是不可重入的。</p><h4 id="乐观读锁tryOptimisticRead"><a href="#乐观读锁tryOptimisticRead" class="headerlink" title="乐观读锁tryOptimisticRead"></a>乐观读锁tryOptimisticRead</h4><p>没有使用cas设置锁的状态，获取读锁只使用位运算检验。在进行具体操作数据前需要调用volidate方法验证stamp是否可用，复制一份要操作的变量到方法栈，也就是操作一个快照。</p><h2 id="第七章"><a href="#第七章" class="headerlink" title="第七章"></a>第七章</h2><p>阻塞队列使用锁来实现，非阻塞队列使用CAS非阻塞算法实现</p><h3 id="ConcurrentLinkedQueue"><a href="#ConcurrentLinkedQueue" class="headerlink" title="ConcurrentLinkedQueue"></a>ConcurrentLinkedQueue</h3><p>线程安全的无界无阻塞队列，底层是单向链表。结点类型为Node，内部维护了一个volatile修饰的变量item，用来存放结点的值，next存放链表的下一结点。</p><h4 id="offer-操作"><a href="#offer-操作" class="headerlink" title="offer 操作"></a>offer 操作</h4><p>在队列末尾添加一个元素<br>通过cas操作来控制某时只有一个线程可以追加元素到队列末尾。竞争失败的线程会通过循环一次次尝试进行cas操作，直到成功才返回，即通过使用无限循环不断进行cas尝试方式来替代阻塞算法挂起调用线程。</p><h4 id="add-操作"><a href="#add-操作" class="headerlink" title="add 操作"></a>add 操作</h4><p>在内部调用的还是offer方法</p><h4 id="poll-操作"><a href="#poll-操作" class="headerlink" title="poll 操作"></a>poll 操作</h4><p>在队列头部获取并移除一个元素，只是通过使用cas将当前结点的item值设置为null，然后重新设置头结点，被移除的结点在垃圾回收时被回收掉。</p><h4 id="peek-操作"><a href="#peek-操作" class="headerlink" title="peek 操作"></a>peek 操作</h4><p>不同于poll，只是获取而不移除队列头部的一个元素，第一次调用时会删除哨兵结点，也就是 next 为 null 的结点，此后head才会指向第一个真正的队列元素。</p><h4 id="remove-操作"><a href="#remove-操作" class="headerlink" title="remove 操作"></a>remove 操作</h4><p>删除元素，存在即删除，存在多个则删除第一个。</p><h4 id="size-操作"><a href="#size-操作" class="headerlink" title="size 操作"></a>size 操作</h4><p>计算当前队列元素个数，存在线程安全问题，cas没有加锁，计数不准确。</p><h4 id="contains-操作"><a href="#contains-操作" class="headerlink" title="contains 操作"></a>contains 操作</h4><p>判断队列中是否含有指定对象，存在线程安全问题。</p><h3 id="LinkedBlockingQueue"><a href="#LinkedBlockingQueue" class="headerlink" title="LinkedBlockingQueue"></a>LinkedBlockingQueue</h3><p>亦是用单向链表实现的，结点类型为Node，但是有一个 初始值为0的原子变量count，用来记录队列元素个数；两个ReentrantLock实例 takeLock和putLock，分别用来作为独占锁控制元素出队和入队的原子性操作；两个条件变量notEmpty、notFull，它们内部有一个条件队列分别用来存放出队和入队时阻塞的线程。</p><p>当某线程要插入新元素但是队列已满，那么阻塞并将其放入notFull内部维护的条件队列中进行等待，直到有线程取出了元素并且调用了notFull的signal方法（表示现在已经不是满的）；<br>当某线程需要从队列取出元素但是队列是空的，那么阻塞并将其放入notEmpty内部维护的条件队列中进行等待，直到有线程插入了元素并且调用了notEmpty的signal方法（表示现在已经不是空的了）。</p><h4 id="独占锁创建过程"><a href="#独占锁创建过程" class="headerlink" title="独占锁创建过程"></a>独占锁创建过程</h4><h5 id="takeLock"><a href="#takeLock" class="headerlink" title="takeLock"></a>takeLock</h5><p>执行take、poll等操作时需要获取takeLock，获取到锁，保证同时只有一个线程能操作链表头结点，当队列为空，执行出队操作的线程会被放入到notEmpty条件队列中。</p><h5 id="putLock"><a href="#putLock" class="headerlink" title="putLock"></a>putLock</h5><p>执行put、offer等操作时需要获取takeLock，获取到锁，保证同时只有一个线程能操作链表尾结点，当队列为满，执行入队操作的线程会被放入到notFull条件队列中。</p><h4 id="offer-操作-1"><a href="#offer-操作-1" class="headerlink" title="offer 操作"></a>offer 操作</h4><p>入队，在队列尾部插入一个元素，该方法是非阻塞的，插入成功返回true，插入失败则丢弃此元素。</p><h4 id="put-操作"><a href="#put-操作" class="headerlink" title="put 操作"></a>put 操作</h4><p>与offer操作类似但却是阻塞的，也是在队列尾部插入一个元素，如果队列有空闲则插入后直接返回，如果队列已满则阻塞当前线程直到有空闲才插入成功后返回。</p><h4 id="poll-操作-1"><a href="#poll-操作-1" class="headerlink" title="poll 操作"></a>poll 操作</h4><p>出队，从队列头部获取并移除一个元素，该方法是非阻塞的。</p><h4 id="take-操作"><a href="#take-操作" class="headerlink" title="take 操作"></a>take 操作</h4><p>出队，从队列头部获取并移除一个元素，该方法是阻塞的，队列为空则阻塞直至队列不空然后返回元素。</p><h4 id="peek-操作-1"><a href="#peek-操作-1" class="headerlink" title="peek 操作"></a>peek 操作</h4><p>从队列头部获取一个元素，该方法是非阻塞的。</p><h4 id="remove-操作-1"><a href="#remove-操作-1" class="headerlink" title="remove 操作"></a>remove 操作</h4><p>删除队列里面指定的元素。删除指定元素前加了两把锁，所以是线程安全的，此时其他调用入队、出队的线程都会被阻塞。删除元素后，如果当前队列有空闲空间，那么唤醒notFull条件队列中的一个因调用put操做入队而阻塞的线程。</p><h4 id="size-操作-1"><a href="#size-操作-1" class="headerlink" title="size 操作"></a>size 操作</h4><p>获取队列的个数。相比ConcurrentLinkedQueue获取的数据更为准确，因为ConcurrentBlockingQueue入队出队都会上锁。</p><h3 id="ArrayBlockingQueue"><a href="#ArrayBlockingQueue" class="headerlink" title="ArrayBlockingQueue"></a>ArrayBlockingQueue</h3><p>有界数组的方式实现的阻塞队列，内部有一个数组items用来存放队列元素。有一个独占锁lock用来保证出、入队操作的原子性，这保证了同时只有一个线程可以进行入队、出队操作。</p><h4 id="offer-操作-2"><a href="#offer-操作-2" class="headerlink" title="offer 操作"></a>offer 操作</h4><p>非阻塞的，向队列尾部插入一个元素。<br>入队enqueue方法，先把当前元素放入数组，然后计算下一个元素应该存放的下标位置，并递增元素个数计数器，最后激活notEmpty的条件队列中因为调用take操作而被阻塞的一个线程。</p><h4 id="put-操作-1"><a href="#put-操作-1" class="headerlink" title="put 操作"></a>put 操作</h4><p>阻塞的，向队列尾部插入一个元素。</p><h4 id="poll-操作-2"><a href="#poll-操作-2" class="headerlink" title="poll 操作"></a>poll 操作</h4><p>非阻塞的，向队列头部获取并移除一个元素。<br>获取元素存到局部变量，然后重置队头元素为null，重新设置队头下标，递减元素计数器，最后激活notFull的条件队列中因为调用put方法而被阻塞的线程。</p><h4 id="take-操作-1"><a href="#take-操作-1" class="headerlink" title="take 操作"></a>take 操作</h4><p>阻塞的，向队列头部获取并移除一个元素。<br>与poll不同的是，如果队列为空那么把当前线程挂起后放入notEmpty的条件队列。</p><h4 id="peek-操作-2"><a href="#peek-操作-2" class="headerlink" title="peek 操作"></a>peek 操作</h4><p>非阻塞的，从队列头部获取一个元素但不移除。</p><h4 id="size-操作-2"><a href="#size-操作-2" class="headerlink" title="size 操作"></a>size 操作</h4><p>获取锁后计算当前队列元素个数，返回前释放锁。</p><h3 id="PriorityBlockingQueue"><a href="#PriorityBlockingQueue" class="headerlink" title="PriorityBlockingQueue"></a>PriorityBlockingQueue</h3><ul><li>带优先级的无界阻塞队列，每次出队都返回优先级最高或者最低的元素。</li><li>内部使用平衡二叉堆实现。</li><li>数组queue用来存放队列元素。有一个allocationSpinLock自旋锁，使用CAS操作来保证同时只有一个线程可以扩容队列。</li><li>比较器comparator用来比较元素大小。</li><li>lock独占锁对象用来控制同时只有一个线程可以进行入队、出队操作。没有notFull条件变量，因为是无界队列。有notEmpty条件变量，用来实现take方法阻塞模式。</li></ul><h4 id="offer-操作-3"><a href="#offer-操作-3" class="headerlink" title="offer 操作"></a>offer 操作</h4><p>在队列中插入一个元素。</p><h5 id="offer-E-e"><a href="#offer-E-e" class="headerlink" title="offer(E e)"></a>offer(E e)</h5><ol><li>首先，方法会检查元素 e 是否为 null，如果是则抛出 NullPointerException 异常。 </li><li>然后，方法会获取队列的锁，以确保在操作过程中不会被其他线程干扰。 </li><li>接着，方法会定义变量 n、cap、es，分别表示队列的大小、容量和元素数组。然后使用 while 循环来检查队列是否已满，如果已满则调用 tryGrow(es, cap) 方法进行扩容，直到队列容量足够插入元素为止。 </li><li>如果队列未满，则需要插入元素。首先判断 comparator 是否为 null，如果为 null 则使用 siftUpComparable(n, e, es) 方法将元素插入队列，如果不为 null 则使用 siftUpUsingComparator(n, e, es, cmp) 方法。这两个方法都是将元素插入到队列末尾，然后根据元素的优先级向上调整，直到找到它在队列中的正确位置。 </li><li>插入元素后，需要更新队列的大小，并使用 notEmpty.signal() 通知等待的 take() 操作可以获取元素了。 </li><li>最后，释放锁，并返回 true 表示插入成功。</li></ol><h5 id="tryGrow-Object-array-int-oldCap"><a href="#tryGrow-Object-array-int-oldCap" class="headerlink" title="tryGrow(Object[] array, int oldCap)"></a>tryGrow(Object[] array, int oldCap)</h5><p>tryGrow() 是 PriorityBlockingQueue 的一个私有方法，用于扩容队列的数组。这个方法会在队列已满时自动调用，以确保队列有足够的空间可以插入元素。<br>下面是方法的具体实现代码：</p><ol><li>首先，方法会释放队列的锁，以允许其他线程在扩容期间访问队列。 </li><li>然后，方法会尝试获取 allocationSpinLock，这是一个自旋锁，用于控制在同一时间只有一个线程可以进行扩容操作。如果当前没有其他线程正在进行扩容，则当前线程可以获取到锁，并开始扩容操作。 </li><li>扩容操作会计算新的容量 newCap，根据当前队列容量的大小进行扩容。如果队列较小，则新容量会增加 2 个元素，以加快队列的增长速度；如果队列较大，则新容量会增加原来容量的一半。然后检查新容量是否超过了 MAX_ARRAY_SIZE 的限制，如果超过了，则抛出 OutOfMemoryError 异常。如果新容量合法，则创建一个新数组 newArray，其长度为 newCap。 </li><li>扩容操作完成后，释放 allocationSpinLock，以允许其他线程进行扩容操作。 </li><li>如果当前线程成功创建了新数组，则需要将原数组中的元素复制到新数组中。由于在释放锁后可能有其他线程修改了队列的状态，因此需要在获取锁之前进行 Thread.yield()，以等待其他线程完成扩容操作。 </li><li>最后，重新获取锁，并将新数组设置为队列的底层数据结构，同时将原数组中的元素复制到新数组中。</li></ol><p>整个扩容操作需要注意多线程并发访问的问题，因此需要采取一系列同步措施，以确保扩容操作能够顺利进行。这些措施包括使用自旋锁进行同步、释放锁以允许其他线程访问队列、在获取锁之前进行 Thread.yield() 以等待其他线程完成扩容操作等。同时，也需要进行一些安全性检查，例如检查新容量是否合法，以防止数组溢出或者内存泄漏等问题。</p><h5 id="siftUpComparable-int-k-T-x-Object-es"><a href="#siftUpComparable-int-k-T-x-Object-es" class="headerlink" title="siftUpComparable(int k, T x, Object[] es)"></a>siftUpComparable(int k, T x, Object[] es)</h5><p><code>siftUpComparable</code> 方法是 <code>PriorityBlockingQueue</code> 类中的一个私有方法，用于将新的元素 <code>x</code> 插入到底层数组 <code>es</code> 中的指定位置 <code>k</code>，并在插入后保持堆的有序性。<br>此方法实现了对 <code>Comparable</code> 接口的支持，即如果元素类型 <code>T</code> 实现了 <code>Comparable</code> 接口，则优先使用其自然排序的方式来保持堆的有序性。<br>具体实现思路为：</p><ol><li>首先将待插入的元素 <code>x</code> 强制转换为 <code>Comparable&lt;? super T&gt;</code> 类型，以方便进行比较。</li><li>从位置 <code>k</code> 开始向上逐层比较和交换元素，直到 <code>x</code> 找到了它在堆中的正确位置。</li><li>比较的方式是将 <code>x</code> 和其父节点的元素进行比较，如果 <code>x</code> 比父节点小，则将父节点下移，同时更新 <code>k</code> 的值为当前父节点的位置。</li><li>如果 <code>x</code> 比父节点大，则说明 <code>x</code> 已经在正确的位置上，插入操作结束。</li></ol><p>最后，将 <code>x</code> 插入到最终的位置 <code>k</code> 上，完成插入操作。</p><h4 id="poll-操作-3"><a href="#poll-操作-3" class="headerlink" title="poll 操作"></a>poll 操作</h4><p>获取独占锁，再获取队列内部堆的根节点元素。</p><h5 id="dequeue"><a href="#dequeue" class="headerlink" title="dequeue()"></a>dequeue()</h5><p><code>dequeue</code> 方法是 <code>PriorityBlockingQueue</code> 类中的一个私有方法，用于从底层数组中删除并返回队首元素，并在删除后保持堆的有序性。<br>具体实现思路为：</p><ol><li>首先从底层数组 <code>es</code> 中取出队首元素，即位置为 <code>0</code> 的元素，并保存到 <code>result</code> 中。</li><li>如果队首元素不为空，则需要进行删除操作： <ol><li>首先从底层数组中取出最后一个元素 <code>x</code>，并将其保存到 <code>es</code> 的最后一个位置上。</li><li>然后将队列大小 <code>size</code> 减一，并将队尾位置 <code>n</code> 更新为新的 <code>size</code>。</li><li>如果队列不为空，则需要将新的队首元素 <code>x</code> 向下逐层比较和交换元素，以保持堆的有序性。</li><li>比较的方式和插入操作中的一致，即如果元素类型 <code>T</code> 实现了 <code>Comparable</code> 接口，则优先使用其自然排序的方式来保持堆的有序性；否则使用 <code>Comparator</code> 接口提供的比较方法。</li><li>比较的起点是位置 <code>0</code>，每次从两个子节点中选择较小的一个进行比较和交换，直到 <code>x</code> 找到了它在堆中的正确位置。</li></ol></li><li>最后将队首元素 <code>result</code> 返回，完成删除操作。</li></ol><p>需要注意的是，该方法在执行时必须要求持有锁，因此没有在方法签名中标注 <code>synchronized</code> 或 <code>ReentrantLock</code> 等关键字。</p><h5 id="siftDownComparable-int-k-T-x-Object-es-int-n"><a href="#siftDownComparable-int-k-T-x-Object-es-int-n" class="headerlink" title="siftDownComparable(int k, T x, Object[] es, int n)"></a>siftDownComparable(int k, T x, Object[] es, int n)</h5><p><code>siftDownComparable</code> 方法用于在 <code>PriorityBlockingQueue</code> 中下滤元素以满足堆的性质。这个方法的目的是将元素 <code>x</code>（在 <code>PriorityBlockingQueue</code> 中位于索引 <code>k</code>）下移，直到它在堆中的位置正确为止。<br>该方法使用了以下参数：</p><ul><li><code>k</code>：元素 <code>x</code> 在 <code>PriorityBlockingQueue</code> 中的索引。</li><li><code>x</code>：需要下移的元素。</li><li><code>es</code>：元素数组。</li><li><code>n</code>：<code>PriorityBlockingQueue</code> 中元素的数量。</li></ul><p>方法首先将元素 <code>x</code> 转换为可比较的类型，然后执行下述循环：</p><ol><li>定义一个变量 <code>half</code> 表示非叶子节点的数量，其中 <code>n &gt;&gt;&gt; 1</code> 表示右移一位，等价于除以 2。这里的 <code>half</code> 是一个索引值，表示最后一个非叶子节点的索引。</li><li>当 <code>k</code> 小于 <code>half</code> 时，进入循环。因为在 <code>PriorityBlockingQueue</code> 中，所有的叶子节点都出现在最后一层或者倒数第二层，所以只有索引小于 <code>half</code> 的节点才有可能是非叶子节点。</li><li>假设当前节点的左孩子为最小节点，将其索引赋值给变量 <code>child</code>。</li><li>如果当前节点存在右孩子，检查右孩子是否比左孩子小，如果是，将右孩子的索引赋值给变量 <code>child</code>。</li><li>如果 <code>key</code> 比 <code>child</code> 大或相等，则跳出循环。</li><li>否则，将 <code>child</code> 的值赋给 <code>es[k]</code>，更新 <code>k</code> 和 <code>child</code> 的值。</li><li>返回 <code>es[k]</code>。</li></ol><p>在循环结束时，元素 <code>x</code> 将位于堆中正确的位置，即节点 <code>k</code>。最后将 <code>x</code> 放置在节点 <code>k</code> 处，并返回 <code>es[k]</code>，即原先位于节点 <code>k</code> 的元素，以便维护堆的有序性质。</p><h4 id="put-操作-2"><a href="#put-操作-2" class="headerlink" title="put 操作"></a>put 操作</h4><p>非阻塞，内部调用offer操作</p><h4 id="take-操作-2"><a href="#take-操作-2" class="headerlink" title="take 操作"></a>take 操作</h4><p>获取队列内部堆树的根节点元素，队列为空则阻塞。知道有线程调用了offer方法，就会激活一个阻塞在notEmpty的条件队列里面的一个线程。</p><h4 id="size-操作-3"><a href="#size-操作-3" class="headerlink" title="size 操作"></a>size 操作</h4><p>同ArrayBlockingQueue</p><h3 id="DelayQueue"><a href="#DelayQueue" class="headerlink" title="DelayQueue"></a>DelayQueue</h3><ul><li>无界阻塞延迟队列，队列中每个元素都有过期时间，只有过期元素才会出队列，队头元素是最快要过期的元素。</li><li>内部使用PriorityQueue存放数据，使用ReentrantLock实现线程同步。</li><li>队列里面的元素需要实现Delayed接口。</li></ul><p>条件变量available与lock锁是对应的，实现线程同步。<br>变量leader的使用是基于Leader-Follower模式的变体，用于减少不必要的线程等待。</p><p>Leader-Follower模式是一种常用的并发模式，也被称为”Reactor模式”。它是基于事件驱动模型的一种并发编程模式，主要用于构建高并发的网络应用程序。<br>在该模式中，系统中有两个主要的角色：Leader和Follower。Leader负责管理整个系统的状态和处理核心业务逻辑，Follower则负责处理请求和响应。<br>具体来说，当有新的请求到达系统时，Follower会将请求放入一个请求队列中，并通过某种方式通知Leader有新的请求需要处理。Leader则从请求队列中获取请求并进行处理，处理完成后将响应返回给对应的Follower进行发送。<br>这种Leader-Follower的架构可以有效地解决高并发网络应用程序中的性能瓶颈问题。通过将请求处理和响应发送分离，可以避免阻塞或延迟等问题的发生。同时，通过多个Follower的协作，可以进一步提高系统的吞吐量和响应能力。<br>不过，需要注意的是，在Leader-Follower模式中，Leader作为整个系统的中心节点，可能成为单点故障，因此需要考虑如何保证Leader的高可用性和容错性。此外，Leader和Follower之间的通信也需要进行一定的优化和协调，以避免性能瓶颈的发生。</p><h4 id="offer-操作-4"><a href="#offer-操作-4" class="headerlink" title="offer 操作"></a>offer 操作</h4><p>插入元素到队列。<br>在 <code>offer</code> 方法中，首先获取锁，然后将元素插入到队列中。如果队列中的第一个元素就是插入的元素，那么就表示需要重置 <code>leader</code>，唤醒等待的线程。最后释放锁并返回插入操作是否成功的结果。</p><h4 id="take-操作-3"><a href="#take-操作-3" class="headerlink" title="take 操作"></a>take 操作</h4><p>获取并移除队列中延迟时间过期的元素，没有的话则等待。</p><ol><li>这段代码是一个 <code>BlockingQueue</code> 的实现，用于在队列中获取元素时进行阻塞等待，直到队列中有元素可以取出。 </li><li><code>ReentrantLock</code> 是一个可重入锁，用于控制多线程访问该队列时的同步问题。 </li><li>代码中的 <code>lock.lockInterruptibly()</code> 是可中断锁，表示在获取锁的过程中，如果当前线程被中断，则会抛出 <code>InterruptedException</code> 异常。 </li><li><code>q</code> 是队列的实际存储结构，<code>available</code> 是与队列操作相关的条件变量。 </li><li>在 <code>for</code> 循环中，首先获取队列头元素 <code>first</code>，如果队列为空，则当前线程通过调用 <code>available.await()</code> 方法进行等待，直到有元素可取。 </li><li>如果队列不为空，则计算队头元素的剩余等待时间 <code>delay</code>。如果 <code>delay &lt;= 0</code>，说明队头元素已经可以被取出，此时直接调用 <code>q.poll()</code> 取出元素并返回。 </li><li>如果 <code>delay &gt; 0</code>，说明队头元素还需要等待一段时间才能被取出。此时将 <code>first</code> 设为 <code>null</code>，以便在等待过程中不会出现引用泄漏的问题。 </li><li>如果此时已经有一个线程作为 <code>leader</code>，说明有其他线程正在等待元素，当前线程需要等待 <code>leader</code> 线程操作完后再进行操作。 </li><li>如果没有 <code>leader</code>，则当前线程将自己作为 <code>leader</code>，并通过 <code>available.awaitNanos(delay)</code> 方法进行等待。等待结束后，将 <code>leader</code> 重新设为 <code>null</code>，表示当前线程不再是 <code>leader</code>。 </li><li>在 <code>finally</code> 块中，判断当前线程是否为 <code>leader</code>，如果是则将 <code>leader</code> 设为 <code>null</code>，表示当前线程已经完成操作。如果此时队列不为空，则通过 <code>available.signal()</code> 唤醒其他等待的线程。最后，释放锁。</li></ol><h4 id="poll-操作-4"><a href="#poll-操作-4" class="headerlink" title="poll 操作"></a>poll 操作</h4><p>获取并移除队头过期的元素，没有过期元素则返回null。</p><h4 id="size-操作-4"><a href="#size-操作-4" class="headerlink" title="size 操作"></a>size 操作</h4><p>计算队列元素，包括过期和未过期的。</p><h3 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h3><p>第七章看的云里雾里，需要仔细揣摩。</p><h2 id="第八章"><a href="#第八章" class="headerlink" title="第八章"></a>第八章</h2><h3 id="线程池解决的问题"><a href="#线程池解决的问题" class="headerlink" title="线程池解决的问题"></a>线程池解决的问题</h3><ol><li>执行大量异步任务时线程池能够提供较好的性能。因为创建、销毁线程池是需要开销的；线程池里面的线程可以复用。</li><li>线程池提供了一种资源限制和管理的手段。</li></ol><h3 id="ThreadPoolExecutor-类概述"><a href="#ThreadPoolExecutor-类概述" class="headerlink" title="ThreadPoolExecutor 类概述"></a>ThreadPoolExecutor 类概述</h3><p>Integer类型的原子变量ctl，用来记录线程池状态和线程池中线程的个数。高3位用来表示线程池状态，低29位用来记录线程池中线程个数。</p><h4 id="线程池状态"><a href="#线程池状态" class="headerlink" title="线程池状态"></a>线程池状态</h4><ul><li>RUNNING：接收新任务并且处理阻塞队列中的任务</li><li>SHUTDOWN：拒绝新任务但是处理阻塞队列里的任务。</li><li>STOP：拒绝新任务并且抛弃阻塞队列里的任务，同时会中断正在处理的任务。</li><li>TIDYING：所有任务都执行完（包含阻塞队列中的任务）后当前线程池活动线程数为0。</li><li>TERMINATED：终止状态。</li></ul><h4 id="线程池参数"><a href="#线程池参数" class="headerlink" title="线程池参数"></a>线程池参数</h4><ul><li>corePoolSize：线程池的核心线程数，即保持活跃的线程数，即使它们闲置也不会被回收。</li><li>maximumPoolSize：线程池的最大线程数，包括核心线程和临时线程。当任务队列满了之后，临时线程会被创建来处理任务，直到达到最大线程数。</li><li>keepAliveTime：当线程池中的线程数大于核心线程数时，临时线程的最大存活时间。如果线程在这段时间内没有执行任何任务，它将被终止。</li><li>unit：keepAliveTime的时间单位。</li><li>workQueue：任务队列，用于存储待执行的任务。线程池中的线程将从该队列中取出任务进行执行。</li><li>threadFactory：用于创建新线程的工厂。</li><li>handler：当任务队列和线程池都已满时，用于处理新任务的拒绝策略。</li></ul><p>下面是一个使用线程工厂创建线程池的例子：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">import</span> java.util.concurrent.*;<br><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">ThreadFactoryExample</span> &#123;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">main</span><span class="hljs-params">(String[] args)</span> &#123;<br>        <span class="hljs-comment">// 创建线程工厂</span><br>        <span class="hljs-type">ThreadFactory</span> <span class="hljs-variable">threadFactory</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">ThreadFactory</span>() &#123;<br>            <span class="hljs-meta">@Override</span><br>            <span class="hljs-keyword">public</span> Thread <span class="hljs-title function_">newThread</span><span class="hljs-params">(Runnable r)</span> &#123;<br>                <span class="hljs-comment">// 创建一个新线程</span><br>                <span class="hljs-type">Thread</span> <span class="hljs-variable">thread</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Thread</span>(r);<br>                <span class="hljs-comment">// 设置线程的名称</span><br>                thread.setName(<span class="hljs-string">&quot;MyThread&quot;</span>);<br>                <span class="hljs-comment">// 设置线程为后台线程</span><br>                thread.setDaemon(<span class="hljs-literal">true</span>);<br>                <span class="hljs-keyword">return</span> thread;<br>            &#125;<br>        &#125;;<br><br>        <span class="hljs-comment">// 创建线程池，使用上面创建的线程工厂</span><br>        <span class="hljs-type">ExecutorService</span> <span class="hljs-variable">executorService</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">ThreadPoolExecutor</span>(<span class="hljs-number">2</span>, <span class="hljs-number">2</span>, <span class="hljs-number">0L</span>, TimeUnit.MILLISECONDS,<br>                <span class="hljs-keyword">new</span> <span class="hljs-title class_">LinkedBlockingQueue</span>&lt;Runnable&gt;(), threadFactory);<br><br>        <span class="hljs-comment">// 提交任务</span><br>        executorService.submit(<span class="hljs-keyword">new</span> <span class="hljs-title class_">MyRunnable</span>());<br>        executorService.submit(<span class="hljs-keyword">new</span> <span class="hljs-title class_">MyRunnable</span>());<br><br>        <span class="hljs-comment">// 关闭线程池</span><br>        executorService.shutdown();<br>    &#125;<br>&#125;<br><br><span class="hljs-keyword">class</span> <span class="hljs-title class_">MyRunnable</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">Runnable</span> &#123;<br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">run</span><span class="hljs-params">()</span> &#123;<br>        System.out.println(Thread.currentThread().getName() + <span class="hljs-string">&quot; is running.&quot;</span>);<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>在这个例子中，我们创建了一个线程工厂，用于创建后台线程，并设置线程名称为”MyThread”。然后，我们使用创建的线程工厂创建了一个线程池，并向其中提交了两个任务。最后，我们关闭了线程池。</p><p>运行这个例子后，会输出两行类似于”MyThread-1 is running.”的信息，表示两个任务都在后台线程中运行。通过使用线程工厂，我们可以更好地控制线程的创建和行为，使得线程池的使用更加灵活和高效。</p><h4 id="线程池类型"><a href="#线程池类型" class="headerlink" title="线程池类型"></a>线程池类型</h4><ul><li><p>newFixedThreadPool:</p><ol><li><code>corePoolSize</code>和<code>maximumPoolSize</code>均为<code>nThreads</code>，即线程池中线程的数量始终为<code>nThreads</code>。</li><li><code>keepAliveTime</code>为0，即当线程池中的线程数量大于<code>corePoolSize</code>时，多余的空闲线程会立即被回收。</li><li><code>unit</code>为<code>TimeUnit.MILLISECONDS</code>，即时间单位为毫秒。</li><li><code>workQueue</code>为<code>LinkedBlockingQueue</code>，即任务队列采用一个无界的、FIFO（先进先出）的链表实现。</li><li><code>threadFactory</code>为默认的线程工厂，即<code>Executors.defaultThreadFactory()</code>方法创建线程。</li><li><code>handler</code>为默认的拒绝策略，即<code>AbortPolicy</code>。</li></ol></li><li><p>newSingleThreadExecutor:</p><ol><li><code>corePoolSize</code>: 线程池中的核心线程数，即线程池中始终保持的线程数量。</li><li><code>maximumPoolSize</code>: 线程池中允许的最大线程数，即线程池中线程数量的上限。当任务数量超过<code>corePoolSize</code>并且任务队列已满时，线程池会创建新的线程，直到线程数量达到<code>maximumPoolSize</code>。</li><li><code>keepAliveTime</code>: 线程池中的线程空闲时间，即当线程池中的线程数量大于<code>corePoolSize</code>时，多余的空闲线程在被回收前等待新任务的最长时间。</li><li><code>unit</code>: 时间单位，与<code>keepAliveTime</code>一起使用。</li><li><code>workQueue</code>: 任务队列，用于保存等待执行的任务。</li></ol><p><code>corePoolSize</code>和<code>maximumPoolSize</code>均为1，即线程池中只有一个线程，<code>keepAliveTime</code>为0，即当线程池中的线程数量大于<code>corePoolSize</code>时，多余的空闲线程会立即被回收，<code>unit</code>为<code>TimeUnit.MILLISECONDS</code>，即时间单位为毫秒。任务队列采用一个无界的、FIFO（先进先出）的链表实现。而线程工厂和拒绝策略均采用默认的实现方式。</p></li><li><p>newCachedThreadPool:</p><ol><li><code>corePoolSize</code>: 线程池中的核心线程数，即线程池中始终保持的线程数量。在这个方法中，<code>corePoolSize</code>被设置为0，因此线程池中不会有任何线程处于等待状态。</li><li><code>maximumPoolSize</code>: 线程池中允许的最大线程数，即线程池中线程数量的上限。在这个方法中，<code>maximumPoolSize</code>被设置为<code>Integer.MAX_VALUE</code>，表示线程池中可以创建的线程数量没有上限。</li><li><code>keepAliveTime</code>: 线程池中的线程空闲时间，即当线程池中的线程数量大于<code>corePoolSize</code>时，多余的空闲线程在被回收前等待新任务的最长时间。</li><li><code>unit</code>: 时间单位，与<code>keepAliveTime</code>一起使用。</li><li><code>workQueue</code>: 任务队列，用于保存等待执行的任务。在这个方法中，任务队列采用一个没有容量的同步队列<code>SynchronousQueue</code>实现，当有新任务到来时，必须要有空闲的线程去执行，否则新任务就会被拒绝。</li></ol><p><code>corePoolSize</code>被设置为0，即线程池中不会有任何线程处于等待状态，而任务队列采用一个没有容量的同步队列<code>SynchronousQueue</code>实现，用于保存等待执行的任务。当有新任务到来时，必须要有空闲的线程去执行，否则新任务就会被拒绝。<code>keepAliveTime</code>被设置为60秒，表示线程池中的空闲线程最多等待60秒后就会被回收。<code>unit</code>为<code>TimeUnit.SECONDS</code>，即时间单位为秒。而线程工厂和拒绝策略均采用默认的实现方式。</p></li></ul><h3 id="源码分析"><a href="#源码分析" class="headerlink" title="源码分析"></a>源码分析</h3><h4 id="execute-Runnable-command"><a href="#execute-Runnable-command" class="headerlink" title="execute(Runnable command)"></a>execute(Runnable command)</h4><p>该方法用于向线程池提交一个任务。</p><ol><li><p>execute 方法会首先检查任务是否为 null，如果是，就会抛出 NullPointerException 异常。然后，它会读取线程池的当前状态（包括线程数、队列中的任务数、状态等）。</p></li><li><p>如果当前线程数小于 corePoolSize，就会尝试使用 addWorker 方法添加一个新的工作线程来执行任务。addWorker 方法会自旋地尝试增加工作线程数量，直到增加成功或者失败。</p></li><li><p>如果队列可以接受新任务（队列大小未满），就会将任务加入到队列中。</p></li><li><p>二次检查，如果此时线程池的状态已经发生了改变（例如，线程池已经关闭或停止），则可能需要将任务从队列中移除并拒绝执行；如果当前线程池为空那么添加一个线程。</p></li><li><p>如果队列已满，就会尝试增加线程，最多增加到 maximumPoolSize。如果增加线程数失败，则说明线程池已经饱和，此时任务也无法执行，需要将其拒绝。</p></li></ol><h4 id="addWorker-Runnable-firstTask-boolean-core"><a href="#addWorker-Runnable-firstTask-boolean-core" class="headerlink" title="addWorker(Runnable firstTask, boolean core)"></a>addWorker(Runnable firstTask, boolean core)</h4><p><code>ThreadPoolExecutor</code> 中的一个私有方法，用于向线程池中添加工作线程（Worker）。方法的主要作用是先检查线程池的状态和当前线程数，然后通过一个无限循环尝试添加工作线程，直到成功添加为止。</p><p>下面是具体的步骤：</p><ol><li>首先，通过 <code>ctl.get()</code> 获取当前线程池的控制状态。这个状态是一个 int 类型的值，其中包括线程池的运行状态和当前的工作线程数等信息。</li><li>如果当前状态为 <code>STOP</code> 、<code>TIDYING</code> 或者 <code>TERMINATED</code>；当前线程池的状态为 <code>SHUTDOWN</code> 且任务队列为空，当前线程池的状态为 <code>SHUTDOWN</code> 且 <code>firstTask</code> 不为 null，则直接返回 false，表示无法添加工作线程。</li><li>然后进入一个无限循环，用于尝试添加工作线程。在循环中，先判断当前线程数是否已经达到了最大值。如果是，直接返回 false，表示无法添加工作线程。</li><li>如果当前线程数还没有达到最大值，则通过 <code>compareAndIncrementWorkerCount(c)</code> 尝试增加线程数。如果增加成功，就跳出循环，表示成功添加了一个工作线程；如果增加失败，则重新获取当前状态 <code>c</code> 并继续循环，如果线程池状态变化了则跳到外层循环，否则内层循环继续cas。</li><li>如果成功添加了一个工作线程，则需要将其加入到线程池中。首先创建一个 <code>Worker</code> 对象，然后将其中的线程对象 <code>t</code> 取出来，再通过 <code>mainLock.lock()</code> 获取线程池的锁，确保线程池状态不会被其他线程修改，因为可能有多个线程调用了线程池的execute方法。</li><li>在获取锁后，先重新获取当前的状态 <code>c</code>，然后判断线程池是否仍在运行或者处于 <code>SHUTDOWN</code> 状态但仍有任务需要执行。如果是，则将 <code>Worker</code> 对象添加到线程池的 <code>workers</code> 集合中，并将 <code>workerAdded</code> 标志设置为 true。同时，如果 <code>workers</code> 集合的大小超过了 <code>largestPoolSize</code>，则更新 <code>largestPoolSize</code> 的值。</li><li>通过 <code>workerAdded</code> 标志判断新增线程是否成功。如果需要启动，则通过 <code>t.start()</code> 启动线程，并将 <code>workerStarted</code> 标志设置为 true。</li><li>最后，如果启动线程失败，则调用 <code>addWorkerFailed(w)</code> 方法处理异常情况。如果线程启动成功，则返回 true，表示成功添加了一个工作线程。</li></ol><h2 id="第九章"><a href="#第九章" class="headerlink" title="第九章"></a>第九章</h2><h3 id="ScheduledThreadPoolExecutor"><a href="#ScheduledThreadPoolExecutor" class="headerlink" title="ScheduledThreadPoolExecutor"></a>ScheduledThreadPoolExecutor</h3><p>指定一定延迟时间后或者定时进行任务调度执行的线程池</p><p>ScheduledThreadPoolExecutor继承了ThreadPoolExecutor，实现了ScheduledExecutorService接口，线程池队列是DelayedWorkQueue。</p><p>ScheduledFutureTask是具有返回值的任务，继承自FutureTask。FutureTask内部有变量state用来表示任务的状态，period用来表示任务的类型。</p><h3 id="源码分析-1"><a href="#源码分析-1" class="headerlink" title="源码分析"></a>源码分析</h3><h4 id="schedule-Runnable-command-long-delay-TimeUnit-unit"><a href="#schedule-Runnable-command-long-delay-TimeUnit-unit" class="headerlink" title="schedule(Runnable command, long delay, TimeUnit unit)"></a>schedule(Runnable command, long delay, TimeUnit unit)</h4><p>提交一个延迟执行的任务，任务从提交时间算起延迟单位为unit的delay时间后开始执行。提交的任务不是周期性任务，之后执行一次。</p><h4 id="scheduleAtFixedRate-Runnable-command-long-initialDelay-long-period-TimeUnit-unit"><a href="#scheduleAtFixedRate-Runnable-command-long-initialDelay-long-period-TimeUnit-unit" class="headerlink" title="scheduleAtFixedRate(Runnable command, long initialDelay, long period, TimeUnit unit)"></a>scheduleAtFixedRate(Runnable command, long initialDelay, long period, TimeUnit unit)</h4><p>相对起始时间点以固定频率调用指定的任务。</p><h2 id="第十章"><a href="#第十章" class="headerlink" title="第十章"></a>第十章</h2><h3 id="CountDownLatch"><a href="#CountDownLatch" class="headerlink" title="CountDownLatch"></a>CountDownLatch</h3><h4 id="应用场景"><a href="#应用场景" class="headerlink" title="应用场景"></a>应用场景</h4><p>主线程中开启多个线程去并行执行任务，并且主线程需要等待所以子线程执行完毕后再进行汇总。此前一般都使用线程的join()方法来实现这一点，但join()方法不够灵活，不能满足不同场景的需要。</p><ol><li>只能等待一个线程：join()方法只能等待一个指定的线程执行完毕后再继续执行，不能同时等待多个线程。</li><li>无法控制等待时间：调用join()方法时，主线程会一直等待被调用线程执行完毕，不能设置等待的超时时间。如果被调用线程发生阻塞或死锁，那么主线程就会一直等待，无法继续执行。</li><li>不够灵活：join()方法的调用位置和方式比较固定，无法灵活地控制多个线程的执行顺序和时间，也不能动态地控制等待的线程数量。</li><li>使用线程池来管理线程时一般都是直接添加Runable到线程池，这时候就没有办法再调用线程的join方法了。</li></ol><h4 id="原理探究"><a href="#原理探究" class="headerlink" title="原理探究"></a>原理探究</h4><p>CountDownLatch是使用AQS实现的，实际上是把计数器的值赋给了AQS的状态变量state。</p><h5 id="void-await-方法"><a href="#void-await-方法" class="headerlink" title="void await()方法"></a>void await()方法</h5><p>当前线程阻塞，直到所有线程都调用了CountDownLatch对象的countDown方法后，也就是计数器的值为0才会返回，或者其他线程调用了当前线程的interrupt()方法中断了当前线程然后返回。</p><h5 id="boolean-await-long-timeout-TimeUnit-unit-方法"><a href="#boolean-await-long-timeout-TimeUnit-unit-方法" class="headerlink" title="boolean await(long timeout, TimeUnit unit)方法"></a>boolean await(long timeout, TimeUnit unit)方法</h5><p>线程阻塞，直到计数器值为0，或者设置的timeout到了，因超时返回false，或者其他线程中断当前线程。</p><h5 id="void-countDown-方法"><a href="#void-countDown-方法" class="headerlink" title="void countDown()方法"></a>void countDown()方法</h5><p>线程调用后，计数器的值递减，如果计数器的值为0，那么唤醒所有因调用await方法而被阻塞的线程。</p><h5 id="long-getCount-方法"><a href="#long-getCount-方法" class="headerlink" title="long getCount()方法"></a>long getCount()方法</h5><p>获取当前计数器的值，也就是AQS的state的值</p>]]></content>
    
    
    <categories>
      
      <category>Java</category>
      
    </categories>
    
    
    <tags>
      
      <tag>原创</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Hello World</title>
    <link href="/2023/04/30/hello-world/"/>
    <url>/2023/04/30/hello-world/</url>
    
    <content type="html"><![CDATA[<p>Welcome to <a href="https://hexo.io/">Hexo</a>! This is your very first post. Check <a href="https://hexo.io/docs/">documentation</a> for more info. If you get any problems when using Hexo, you can find the answer in <a href="https://hexo.io/docs/troubleshooting.html">troubleshooting</a> or you can ask me on <a href="https://github.com/hexojs/hexo/issues">GitHub</a>.</p><h2 id="Quick-Start"><a href="#Quick-Start" class="headerlink" title="Quick Start"></a>Quick Start</h2><h3 id="Create-a-new-post"><a href="#Create-a-new-post" class="headerlink" title="Create a new post"></a>Create a new post</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">$ hexo new <span class="string">&quot;My New Post&quot;</span></span><br></pre></td></tr></table></figure><p>More info: <a href="https://hexo.io/docs/writing.html">Writing</a></p><h3 id="Run-server"><a href="#Run-server" class="headerlink" title="Run server"></a>Run server</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">$ hexo server</span><br></pre></td></tr></table></figure><p>More info: <a href="https://hexo.io/docs/server.html">Server</a></p><h3 id="Generate-static-files"><a href="#Generate-static-files" class="headerlink" title="Generate static files"></a>Generate static files</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">$ hexo generate</span><br></pre></td></tr></table></figure><p>More info: <a href="https://hexo.io/docs/generating.html">Generating</a></p><h3 id="Deploy-to-remote-sites"><a href="#Deploy-to-remote-sites" class="headerlink" title="Deploy to remote sites"></a>Deploy to remote sites</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">$ hexo deploy</span><br></pre></td></tr></table></figure><p>More info: <a href="https://hexo.io/docs/one-command-deployment.html">Deployment</a></p>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>initial-blog</title>
    <link href="/2023/04/30/initial-blog/"/>
    <url>/2023/04/30/initial-blog/</url>
    
    <content type="html"><![CDATA[<p><img src="/images/0220904110038.jpg"><br><img src="/images/102262100_p0.jpg"></p>]]></content>
    
    
    <categories>
      
      <category>Java</category>
      
    </categories>
    
    
    <tags>
      
      <tag>原创</tag>
      
    </tags>
    
  </entry>
  
  
  
  
</search>
